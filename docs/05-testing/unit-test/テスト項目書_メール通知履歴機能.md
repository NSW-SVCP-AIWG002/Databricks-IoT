# テスト項目書 - メール通知履歴機能 Service層

**対象ファイル:** `tests/unit/services/test_mail_history_service.py`
**対象モジュール:** `iot_app.services.mail_history_service`
**作成日:** 2026-04-17
**更新日:** 2026-04-20
**テスト総数:** 35件

| 項目 | 内容 |
| ---- | ---- |
| 作成者 | - |
| レビュー者 | - |

### 更新履歴

| 日付 | 内容 |
| ---------- | --------------------------------------------------------------------------------------------------------------- |
| 2026-04-17 | 初版作成（26件） |
| 2026-04-20 | レビュー指摘対応: `freeze_time` 追加・`filter_by` 引数検証・`limit()` 検証・第2ソートキー検証・観点番号修正（27件） |
| 2026-04-23 | フロント表示項目の全項目欠損なし検証テストを追加（29件） |
| 2026-04-23 | 上長指摘対応: 異常系・片側日付フィルタ・全フィルタ組み合わせテストを追加（33件） |
| 2026-04-23 | 仕様書・テスト項目書との差異解消: freeze_time記述削除・datetime.combine変換検証テスト追加（35件） |
| 2026-04-23 | 横断レビュー対応: No.3検証記述修正・No.22観点番号修正・TestGetMailHistoryList内No.28の行順序を修正（35件） |

---

## 観点番号の凡例

本書で使用する観点番号は `docs/05-testing/unit-test/unit-test-perspectives.md` に準拠する。
同観点表に対応する分類が存在しない機能固有の観点については「**機能固有**」と明記する。

---

## TestGetMailTypeChoices

`get_mail_type_choices()` のフィルタリング・戻り値を検証するテスト群。

| No  | テスト観点                                      | 操作手順                                                                                                | 予想結果                                                                                        | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 1   | 2.1.1 正常系処理（有効な入力データ）            | `MailTypeMaster.query` をモック化し、`delete_flag=False` の2件を返すよう設定して `get_mail_type_choices()` を呼び出す | 戻り値がモックで設定した2件のリストと一致し、`filter_by(delete_flag=False)` が1回呼ばれること   |          |        |        |      |
| 2   | 3.1.4.2 検索結果戻り値ハンドリング（空結果）    | `MailTypeMaster.query` をモック化し、空リストを返すよう設定して `get_mail_type_choices()` を呼び出す   | 戻り値が空リスト `[]` であること                                                                |          |        |        |      |

---

## TestGetMailTypeById

`get_mail_type_by_id()` のID検索・存在チェックを検証するテスト群。

| No  | テスト観点                                      | 操作手順                                                                                                | 予想結果                                                                                        | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 3   | 2.1.1 正常系処理（有効な入力データ）            | `db.session.get` をモック化し、`mail_type_id=1` に対応するレコードを返すよう設定して `get_mail_type_by_id(1)` を呼び出す | 戻り値がモックレコードと一致し、`db.session.get(MailTypeMaster, 1)` が呼ばれること            |          |        |        |      |
| 4   | 2.2.2 対象データ存在チェック（存在しない）      | `MailTypeMaster.query.get` が `None` を返すよう設定して `get_mail_type_by_id(999)` を呼び出す          | 戻り値が `None` であること                                                                      |          |        |        |      |

---

## TestGetSortColumn

`get_sort_column()` のDB取得・フォールバック動作を検証するテスト群。

| No  | テスト観点                                      | 操作手順                                                                                                | 予想結果                                                                                        | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 5   | 2.1.1 正常系処理（有効な入力データ）            | `SortItemMaster.query` をモック化し、`sort_item_name='sent_at'` を返すよう設定して `get_sort_column(3)` を呼び出す | 戻り値が `'sent_at'`（DB から取得した値）であること。`filter_by(view_id=5, sort_item_id=3, delete_flag=False)` が呼ばれること |          |        |        |      |
| 6   | 機能固有: フォールバック動作（sort_id=1）       | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(1)` を呼び出す | 戻り値がフォールバック値 `'mail_type'` であること                                               |          |        |        |      |
| 7   | 機能固有: フォールバック動作（sort_id=2）       | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(2)` を呼び出す | 戻り値がフォールバック値 `'subject'` であること                                                 |          |        |        |      |
| 8   | 機能固有: フォールバック動作（sort_id=3）       | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(3)` を呼び出す | 戻り値がフォールバック値 `'sent_at'` であること                                                 |          |        |        |      |
| 9   | 機能固有: フォールバック動作（未定義 sort_id）  | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(999)` を呼び出す | 戻り値が `None` であること（フォールバック辞書にも存在しない）                                  |          |        |        |      |

---

## TestGetDefaultDateRange

`get_default_date_range()` の日付計算・型を検証するテスト群。期待値は実行時に同じ計算式で動的に導出するため、実行タイミングに依存しない。

| No  | テスト観点                              | 操作手順                                                              | 予想結果                                                             | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | --------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 10  | 機能固有: 日付範囲計算（7日間）         | `get_default_date_range()` を呼び出す | `end - start` が `7日` であること                                    |          |        |        |      |
| 11  | 機能固有: 戻り値型チェック              | `get_default_date_range()` を呼び出す | `start` と `end` がともに `date` 型であること                        |          |        |        |      |
| 12  | 機能固有: 開始日・終了日の前後関係      | `get_default_date_range()` を呼び出す | `start < end` であること                                             |          |        |        |      |
| 13  | 機能固有: 実行日基準の具体値検証        | `get_default_date_range()` を呼び出し、期待値を同じ計算式（`date.today() - timedelta(days=7)`）で動的に導出する | `start == date.today() - timedelta(days=7)`、`end == date.today()` であること |          |        |        |      |

---

## TestGetMailHistoryList

`get_mail_history_list()` の検索フィルタ・ページネーション・ソート・戻り値を検証するテスト群。

| No  | テスト観点                                             | 操作手順                                                                                                                                                  | 予想結果                                                                                                                              | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 14  | 2.1.1 正常系処理（有効な入力データ）                   | `MailHistory.query` をモック化し、モックレコード1件・総件数1を返すよう設定して全条件を空にした `get_mail_history_list()` を呼び出す                        | 戻り値がモックレコードのリストと総件数 `1` のタプルであること                                                                          |          |        |        |      |
| 15  | 3.1.1.1 検索条件指定（organization_id）                | `MailHistory.query` をモック化し、`organization_id=42` で `get_mail_history_list()` を呼び出す                                                            | `filter_by(organization_id=42)` が1回呼ばれること                                                                                    |          |        |        |      |
| 16  | 3.1.1.1 検索条件指定（mail_types）                     | `MailHistory.query` をモック化し、`mail_types=[1, 2]` で `get_mail_history_list()` を呼び出す                                                             | `query.filter` が1回呼ばれること（`mail_type.in_([1, 2])` フィルタが適用される）                                                     |          |        |        |      |
| 17  | 3.1.1.1 検索条件指定（keyword）                        | `MailHistory.query` および `or_` をモック化し、`keyword='テスト'` で `get_mail_history_list()` を呼び出す                                                 | `query.filter` が1回呼ばれること、`or_()` が呼ばれること（件名・本文の OR フィルタが適用される）                                     |          |        |        |      |
| 18  | 3.1.1.1 検索条件指定（日付範囲）                       | `MailHistory.query` をモック化し、`sent_at` の `__ge__`/`__le__` を明示設定した上で `sent_at_start=date(2026,4,1)`, `sent_at_end=date(2026,4,10)` で `get_mail_history_list()` を呼び出す | `query.filter` が2回（開始日・終了日それぞれ1回ずつ）呼ばれること                                                                    |          |        |        |      |
| 19  | 3.1.2.1 検索条件未指定（全件相当）                     | `MailHistory.query` をモック化し、全検索条件を空/None にして `get_mail_history_list()` を呼び出す                                                         | `query.filter` が1回も呼ばれないこと                                                                                                  |          |        |        |      |
| 20  | 3.1.3.1 ページング・件数制御（page=1）                 | `MailHistory.query` をモック化し、`page=1` で `get_mail_history_list()` を呼び出す                                                                        | `query.offset(0)` が呼ばれること（page=1 → offset=0）。`query.limit(_DEFAULT_PER_PAGE)` が呼ばれること                               |          |        |        |      |
| 21  | 3.1.3.1 ページング・件数制御（page=3）                 | `MailHistory.query` をモック化し、`page=3` で `get_mail_history_list()` を呼び出す                                                                        | `query.offset((3-1)*_DEFAULT_PER_PAGE)` が呼ばれること                                                                               |          |        |        |      |
| 22  | 2.1.2 正常系処理（空データ）                           | `MailHistory.query` をモック化し、空リストと総件数 `0` を返すよう設定して `get_mail_history_list()` を呼び出す                                            | 戻り値が空リスト `[]` と総件数 `0` のタプルであること                                                                                 |          |        |        |      |
| 23  | 機能固有: ソート（降順）                               | `MailHistory.query` をモック化し、`sort_column='sent_at'`, `order='desc'` で `get_mail_history_list()` を呼び出す                                         | `sent_at` カラムモックの `.desc()` が1回呼ばれること。第2ソートキーとして `mail_history_id.asc()` が1回呼ばれること                   |          |        |        |      |
| 24  | 機能固有: ソート（昇順）                               | `MailHistory.query` をモック化し、`sort_column='sent_at'`, `order='asc'` で `get_mail_history_list()` を呼び出す                                          | `sent_at` カラムモックの `.asc()` が1回呼ばれること。第2ソートキーとして `mail_history_id.asc()` が1回呼ばれること                    |          |        |        |      |
| 28  | 機能固有: フロント表示項目の全項目欠損なし（一覧）     | `MailHistory.query` をモック化し、`mail_type`, `subject`, `body`, `sent_at`, `mail_history_uuid` を明示設定したレコードを返すよう設定して `get_mail_history_list()` を呼び出す | 取得レコードの `mail_type`, `subject`, `body`, `sent_at`, `mail_history_uuid` がいずれも `None` でないこと                           |          |        |        |      |
| 30  | 機能固有: 異常系 - 無効な sort_column → sent_at フォールバック | `MailHistory` をモック化し、`sort_column='nonexistent_col'`（`MockModel.nonexistent_col = None` で存在しないカラムを再現）で `get_mail_history_list()` を呼び出す | `sent_at` カラムの `.desc()` が呼ばれること（フォールバック確認）。`mail_history_id.asc()` も呼ばれること |          |        |        |      |
| 31  | 3.1.1.1 検索条件指定（sent_at_start のみ）          | `MailHistory.query` をモック化し、`sent_at_start=date(2026,4,1)`, `sent_at_end=None` で `get_mail_history_list()` を呼び出す | `query.filter` が1回（開始日フィルタのみ）呼ばれること（終了日フィルタは適用されない） |          |        |        |      |
| 32  | 3.1.1.1 検索条件指定（sent_at_end のみ）            | `MailHistory.query` をモック化し、`sent_at_start=None`, `sent_at_end=date(2026,4,30)` で `get_mail_history_list()` を呼び出す | `query.filter` が1回（終了日フィルタのみ）呼ばれること（開始日フィルタは適用されない） |          |        |        |      |
| 33  | 2.1.1 全体確認: 全フィルタ同時適用                  | `MailHistory.query` および `or_` をモック化し、`mail_types=[1,2]`, `keyword='テスト'`, `sent_at_start=date(2026,4,1)`, `sent_at_end=date(2026,4,30)` を全て指定して `get_mail_history_list()` を呼び出す | `query.filter` が計4回（mail_type・keyword・開始日・終了日 各1回ずつ）呼ばれること |          |        |        |      |
| 34  | 機能固有: sent_at_start の datetime.combine 変換（time.min） | `MailHistory.query` をモック化し、`sent_at.__ge__` を設定した上で `sent_at_start=date(2026,4,1)`, `sent_at_end=None` で `get_mail_history_list()` を呼び出す | `sent_at.__ge__` が `datetime(2026, 4, 1, 0, 0, 0)`（time.min 付加）で呼ばれること |          |        |        |      |
| 35  | 機能固有: sent_at_end の datetime.combine 変換（23:59:59）   | `MailHistory.query` をモック化し、`sent_at.__le__` を設定した上で `sent_at_start=None`, `sent_at_end=date(2026,4,30)` で `get_mail_history_list()` を呼び出す | `sent_at.__le__` が `datetime(2026, 4, 30, 23, 59, 59)` で呼ばれること |          |        |        |      |

---

## TestGetMailHistoryDetail

`get_mail_history_detail()` の詳細取得・存在チェック・スコープ制限を検証するテスト群。

| No  | テスト観点                                        | 操作手順                                                                                                                                                 | 予想結果                                                                                                                 | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------- | ------ | ------ | ---- |
| 25  | 2.1.1 正常系処理（有効な入力データ）              | `MailHistory.query` をモック化し、`mail_history_uuid='abc-123'`, `organization_id=1` に対応するレコードを返すよう設定して `get_mail_history_detail('abc-123', organization_id=1)` を呼び出す | 戻り値がモックレコードと一致し、`filter_by(mail_history_uuid='abc-123', organization_id=1)` が1回呼ばれること           |          |        |        |      |
| 26  | 2.2.2 対象データ存在チェック（UUID 未存在）       | `MailHistory.query.filter_by.return_value.first` が `None` を返すよう設定して `get_mail_history_detail('no-such-uuid', organization_id=1)` を呼び出す | 戻り値が `None` であること                                                                                               |          |        |        |      |
| 27  | 3.1.1.1 検索条件指定（データスコープ）            | `MailHistory.query.filter_by.return_value.first` が `None` を返すよう設定して `get_mail_history_detail('abc-123', organization_id=999)` を呼び出す      | 戻り値が `None` であり、`filter_by(mail_history_uuid='abc-123', organization_id=999)` が呼ばれること（スコープ制限確認） |          |        |        |      |
| 29  | 機能固有: フロント表示項目の全項目欠損なし（詳細） | `MailHistory.query` をモック化し、`mail_history_id`, `mail_type`, `sender_email`, `recipients`, `sent_at`, `subject`, `body` を明示設定したレコードを返すよう設定して `get_mail_history_detail()` を呼び出す | 取得レコードの `mail_history_id`, `mail_type`, `sender_email`, `recipients`, `sent_at`, `subject`, `body` がいずれも `None` でないこと |          |        |        |      |
