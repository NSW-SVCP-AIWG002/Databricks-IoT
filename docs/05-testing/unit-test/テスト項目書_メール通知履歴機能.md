# テスト項目書 - メール通知履歴機能 Service層

**対象ファイル:** `tests/unit/services/test_mail_history_service.py`
**対象モジュール:** `iot_app.services.mail_history_service`
**作成日:** 2026-04-17
**テスト総数:** 26件

---

## TestGetMailTypeChoices

`get_mail_type_choices()` のフィルタリング・戻り値を検証するテスト群。

| No  | テスト観点                                   | 操作手順                                                                                                | 予想結果                                                                                              | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 1   | 2.1.1 正常系処理（有効な入力データ）         | `MailTypeMaster.query` をモック化し、`delete_flag=False` の2件を返すよう設定して `get_mail_type_choices()` を呼び出す | 戻り値がモックで設定した2件のリストと一致し、`filter_by(delete_flag=False)` が1回呼ばれること         |          |        |        |      |
| 2   | 2.4.1 データフィルタリング（空結果）         | `MailTypeMaster.query` をモック化し、空リストを返すよう設定して `get_mail_type_choices()` を呼び出す   | 戻り値が空リスト `[]` であること                                                                      |          |        |        |      |

---

## TestGetMailTypeById

`get_mail_type_by_id()` のID検索・存在チェックを検証するテスト群。

| No  | テスト観点                                   | 操作手順                                                                                                | 予想結果                                                                                              | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 3   | 2.1.1 正常系処理（有効な入力データ）         | `MailTypeMaster.query.get` をモック化し、`mail_type_id=1` に対応するレコードを返すよう設定して `get_mail_type_by_id(1)` を呼び出す | 戻り値がモックレコードと一致し、`query.get(1)` が呼ばれること                                       |          |        |        |      |
| 4   | 2.2.2 対象データ存在チェック（存在しない）   | `MailTypeMaster.query.get` が `None` を返すよう設定して `get_mail_type_by_id(999)` を呼び出す         | 戻り値が `None` であること                                                                            |          |        |        |      |

---

## TestGetSortColumn

`get_sort_column()` のDB取得・フォールバック動作を検証するテスト群。

| No  | テスト観点                                   | 操作手順                                                                                                | 予想結果                                                                                              | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 5   | 2.1.1 正常系処理（有効な入力データ）         | `SortItemMaster.query` をモック化し、`sort_item_name='sent_at'` を返すよう設定して `get_sort_column(3)` を呼び出す | 戻り値が `'sent_at'`（DB から取得した値）であること                                                  |          |        |        |      |
| 6   | 2.3.1 フォールバック動作（sort_id=1）        | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(1)` を呼び出す | 戻り値がフォールバック値 `'mail_type'` であること                                                    |          |        |        |      |
| 7   | 2.3.2 フォールバック動作（sort_id=2）        | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(2)` を呼び出す | 戻り値がフォールバック値 `'subject'` であること                                                      |          |        |        |      |
| 8   | 2.3.3 フォールバック動作（sort_id=3）        | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(3)` を呼び出す | 戻り値がフォールバック値 `'sent_at'` であること                                                      |          |        |        |      |
| 9   | 2.3.4 フォールバック動作（未定義 sort_id）   | `SortItemMaster.query.filter_by.return_value.first` が `None` を返すよう設定して `get_sort_column(999)` を呼び出す | 戻り値が `None` であること（フォールバック辞書にも存在しない）                                       |          |        |        |      |

---

## TestGetDefaultDateRange

`get_default_date_range()` の日付計算・型を検証するテスト群。

| No  | テスト観点                              | 操作手順                                                                          | 予想結果                                                     | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | --------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------ | -------- | ------ | ------ | ---- |
| 10  | 3.5.1 日付処理（範囲計算）              | `get_default_date_range()` を引数なしで呼び出す                                   | `end - start` が `7日` であること                            |          |        |        |      |
| 11  | 3.5.2 日付処理（型チェック）            | `get_default_date_range()` を引数なしで呼び出す                                   | `start` と `end` がともに `date` 型であること                |          |        |        |      |
| 12  | 3.5.3 日付処理（前後関係）              | `get_default_date_range()` を引数なしで呼び出す                                   | `start < end` であること                                     |          |        |        |      |

---

## TestGetMailHistoryList

`get_mail_history_list()` の検索フィルタ・ページネーション・ソート・戻り値を検証するテスト群。

| No  | テスト観点                                        | 操作手順                                                                                                                                                  | 予想結果                                                                                                                              | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ------ | ---- |
| 13  | 2.1.1 正常系処理（有効な入力データ）              | `MailHistory.query` をモック化し、モックレコード1件・総件数1を返すよう設定して全条件を空にした `get_mail_history_list()` を呼び出す                        | 戻り値がモックレコードのリストと総件数 `1` のタプルであること                                                                          |          |        |        |      |
| 14  | 2.4.1 データフィルタリング（組織スコープ）        | `MailHistory.query` をモック化し、`organization_id=42` で `get_mail_history_list()` を呼び出す                                                            | `filter_by(organization_id=42)` が1回呼ばれること                                                                                    |          |        |        |      |
| 15  | 2.4.2 データフィルタリング（メール種別）          | `MailHistory.query` をモック化し、`mail_types=[1, 2]` で `get_mail_history_list()` を呼び出す                                                             | `query.filter` が1回呼ばれること（`mail_type.in_([1, 2])` フィルタが適用される）                                                     |          |        |        |      |
| 16  | 2.4.3 データフィルタリング（キーワード検索）      | `MailHistory.query` および `or_` をモック化し、`keyword='テスト'` で `get_mail_history_list()` を呼び出す                                                 | `query.filter` が1回呼ばれること、`or_()` が呼ばれること（件名・本文の OR フィルタが適用される）                                     |          |        |        |      |
| 17  | 2.4.4 データフィルタリング（日付範囲）            | `MailHistory.query` をモック化し、`sent_at` の `__ge__`/`__le__` を明示設定した上で `sent_at_start=date(2026,4,1)`, `sent_at_end=date(2026,4,10)` で `get_mail_history_list()` を呼び出す | `query.filter` が2回（開始日・終了日それぞれ1回ずつ）呼ばれること                                                                    |          |        |        |      |
| 18  | 2.4.5 データフィルタリング（条件なし）            | `MailHistory.query` をモック化し、全検索条件を空/None にして `get_mail_history_list()` を呼び出す                                                         | `query.filter` が1回も呼ばれないこと                                                                                                  |          |        |        |      |
| 19  | 3.3.1 ページネーション（page=1）                  | `MailHistory.query` をモック化し、`page=1` で `get_mail_history_list()` を呼び出す                                                                        | `query.offset` が `0` で呼ばれること（page=1 → offset=(1-1)*per_page=0）                                                             |          |        |        |      |
| 20  | 3.3.2 ページネーション（page=3）                  | `MailHistory.query` をモック化し、`page=3` で `get_mail_history_list()` を呼び出す                                                                        | `query.offset` が `(3-1)*_DEFAULT_PER_PAGE` で呼ばれること                                                                           |          |        |        |      |
| 21  | 2.1.2 正常系処理（空結果）                        | `MailHistory.query` をモック化し、空リストと総件数 `0` を返すよう設定して `get_mail_history_list()` を呼び出す                                            | 戻り値が空リスト `[]` と総件数 `0` のタプルであること                                                                                 |          |        |        |      |
| 22  | 3.4.1 ソート（降順）                              | `MailHistory.query` をモック化し、`sort_column='sent_at'`, `order='desc'` で `get_mail_history_list()` を呼び出す                                         | `sent_at` カラムモックの `.desc()` が1回呼ばれること                                                                                  |          |        |        |      |
| 23  | 3.4.2 ソート（昇順）                              | `MailHistory.query` をモック化し、`sort_column='sent_at'`, `order='asc'` で `get_mail_history_list()` を呼び出す                                          | `sent_at` カラムモックの `.asc()` が1回呼ばれること                                                                                   |          |        |        |      |

---

## TestGetMailHistoryDetail

`get_mail_history_detail()` の詳細取得・存在チェック・スコープ制限を検証するテスト群。

| No  | テスト観点                                        | 操作手順                                                                                                                                                 | 予想結果                                                                                                                 | 実施結果 | 確認日 | 確認者 | 備考 |
| --- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------- | ------ | ------ | ---- |
| 24  | 2.1.1 正常系処理（有効な入力データ）              | `MailHistory.query` をモック化し、`mail_history_uuid='abc-123'`, `organization_id=1` に対応するレコードを返すよう設定して `get_mail_history_detail('abc-123', organization_id=1)` を呼び出す | 戻り値がモックレコードと一致し、`filter_by(mail_history_uuid='abc-123', organization_id=1)` が1回呼ばれること           |          |        |        |      |
| 25  | 2.2.2 対象データ存在チェック（UUID 未存在）       | `MailHistory.query.filter_by.return_value.first` が `None` を返すよう設定して `get_mail_history_detail('no-such-uuid', organization_id=1)` を呼び出す | 戻り値が `None` であること                                                                                               |          |        |        |      |
| 26  | 2.4.1 データフィルタリング（組織スコープ不一致）  | `MailHistory.query.filter_by.return_value.first` が `None` を返すよう設定して `get_mail_history_detail('abc-123', organization_id=999)` を呼び出す      | 戻り値が `None` であり、`filter_by(mail_history_uuid='abc-123', organization_id=999)` が呼ばれること（スコープ制限確認） |          |        |        |      |
