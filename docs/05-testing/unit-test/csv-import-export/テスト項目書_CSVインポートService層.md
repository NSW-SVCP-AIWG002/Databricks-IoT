# テスト項目書 - CSV インポート/エクスポート Service層

対象テストコード: `tests/unit/services/test_csv_service.py`
テスト総数: **28件**

---

## TestDetectEncoding

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 2.1.1 正常系処理：有効な入力データ | UTF-8 BOM（0xEF 0xBB 0xBF）で始まるバイト列（`b'\xef\xbb\xbf' + '操作列,デバイスID\n'.encode('utf-8')`）を用意し、`detect_encoding(file_content)` を呼び出す | 戻り値が `'utf-8-sig'` であること |
| 2 | 2.1.1 正常系処理：有効な入力データ | `chardet` モジュールをパッチし `chardet.detect` が `{'encoding': 'UTF-8', 'confidence': 0.99}` を返すよう設定する。BOMなしバイト列（`'操作列,デバイスID\n'.encode('utf-8')`）を用意し、`detect_encoding(file_content)` を呼び出す | chardet が検出したエンコーディング `'UTF-8'` が返されること |
| 3 | 2.1.1 正常系処理：有効な入力データ | `chardet` モジュールをパッチし `chardet.detect` が `{'encoding': 'ascii', 'confidence': 1.0}` を返すよう設定する。ファイルコンテンツ `b'some,csv,content'` を用意し、`detect_encoding(file_content)` を呼び出す | `chardet.detect` がファイルコンテンツ `b'some,csv,content'` を引数として1回呼び出されること |

---

## TestReadCsv

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 4 | 2.1.1 正常系処理：有効な入力データ | ヘッダー1行＋データ1行のCSVバイト列（`'操作列,デバイスID\nR,DEV-001\n'.encode('utf-8')`）を用意し、`read_csv(csv_bytes, 'utf-8')` を呼び出す | pandas DataFrame（`iloc` 属性を持つオブジェクト）が返され、データ行数が1件であること |
| 5 | 2.1.1 正常系処理：有効な入力データ | ヘッダー行に `操作列,デバイスID,デバイス名` を含むCSVバイト列（`'操作列,デバイスID,デバイス名\nR,DEV-001,センサーA\n'.encode('utf-8')`）を用意し、`read_csv(csv_bytes, 'utf-8')` を呼び出す | 戻り値の `columns` に `'操作列'`・`'デバイスID'`・`'デバイス名'` がすべて含まれること |
| 6 | 2.1.2 正常系処理：最小構成の入力 | データ行の間に空行を含むCSVバイト列（`'操作列,デバイスID\nR,DEV-001\n\nR,DEV-002\n'.encode('utf-8')`）を用意し、`read_csv(csv_bytes, 'utf-8')` を呼び出す | 空行がスキップされ、データ行が2件読み込まれること（`len(result) == 2`） |
| 7 | 1.3.6 AppError：予期しない内部エラー発生（CL-2-1: 例外伝播） | Shift-JISエンコードのバイト列（`'テスト'.encode('shift-jis')`）をエンコーディング `'utf-8'` として渡し、`read_csv(file_content, 'utf-8')` を呼び出す | 例外（`Exception`）が発生し、上位へ伝播すること（`pytest.raises(Exception)` でキャッチ） |

---

## TestValidateCsvFormat

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 8 | 2.1.1 正常系処理：有効な入力データ | `db` をパッチし `db.session.query().filter().all()` が `[(1,)]`（組織ID=1のみ）を返すよう設定する。`{'操作列': 'R', '組織ID': '1', 'デバイスID': 'DEV-001', 'デバイス名': 'センサーA'}` を1行含む DataFrame と `organization_id=1` のユーザーモックを用意し、`validate_csv_format(df, 1, user)` を呼び出す | 空リストが返されること（`errors == []`） |
| 9 | 1.1.1 必須チェック：未入力（空文字） | `db` をパッチし組織ID=1を返すよう設定する。デバイス名が空文字（`'デバイス名': ''`）のデータ1行を含む DataFrame と `organization_id=1` のユーザーモックを用意し、`validate_csv_format(df, 1, user)` を呼び出す | エラーリストに1件以上のエラーが返されること（`len(errors) > 0`） |
| 10 | 1.1.1 必須チェック：未入力（空文字） | `db` をパッチし組織ID=1を返すよう設定する。デバイスIDとデバイス名がともに空文字のデータ1行を含む DataFrame と `organization_id=1` のユーザーモックを用意し、`validate_csv_format(df, 1, user)` を呼び出す | エラーリストに1件以上のエラーが返され、先頭エラーに `'row'` キーが存在し、その値が2以上（ヘッダー行分オフセット）であること |
| 11 | 1.1.1 必須チェック：未入力（空文字） | No.10 と同じ設定で `validate_csv_format(df, 1, user)` を呼び出す | エラーリストに1件以上のエラーが返され、先頭エラーに `'column'` キーが存在すること |
| 12 | 1.1.1 必須チェック：未入力（空文字） | No.10 と同じ設定で `validate_csv_format(df, 1, user)` を呼び出す | エラーリストに1件以上のエラーが返され、先頭エラーに `'message'` キーが存在すること |
| 13 | 1.2.2 認可機能：権限なし | `db` をパッチし `db.session.query().filter().all()` が `[(1,)]`（組織ID=1のみ）を返すよう設定する。組織ID=`'99'`（スコープ外）のデータ1行を含む DataFrame と `organization_id=1` のユーザーモックを用意し、`validate_csv_format(df, 1, user)` を呼び出す | エラーリストの中に `message` キーに `'アクセス権限'` を含むエラーが1件以上あること |
| 14 | 1.1.1 必須チェック：未入力（空文字） | `db` をパッチし組織ID=1を返すよう設定する。デバイスIDとデバイス名がともに空文字のデータ2行を含む DataFrame と `organization_id=1` のユーザーモックを用意し、`validate_csv_format(df, 1, user)` を呼び出す | エラーリストに2件以上のエラーが返されること（`len(errors) >= 2`） |
| 15 | 1.3.6 AppError：予期しない内部エラー発生（CL-2-1: 例外伝播） | `db` をパッチし `db.session.query` が `Exception('DB connection error')` を送出するよう設定する。有効なデータ1行を含む DataFrame と `organization_id=1` のユーザーモックを用意し、`validate_csv_format(df, 1, user)` を呼び出す | 例外（`Exception`）が発生し、上位へ伝播すること（`pytest.raises(Exception)` でキャッチ） |

---

## TestApplyDataScopeFilter

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 16 | 1.2.1 認可機能：権限あり | `db` と `OrganizationClosure` をパッチし、`db.session.query().filter().all()` が `[(1,), (2,)]`（組織ID=1,2）を返すよう設定する。`MagicMock` の `query` オブジェクトと `organization_id=1` のユーザーモックを用意し、`apply_data_scope_filter(query, user)` を呼び出す | `query.filter` が1回呼ばれること（フィルタリングが実行されること） |
| 17 | 1.2.2 認可機能：権限なし | `db` と `OrganizationClosure` をパッチし、`db.session.query().filter().all()` が空リスト `[]` を返すよう設定する。`query.filter.return_value = query` と設定し `organization_id=999` のユーザーモックを用意し、`apply_data_scope_filter(query, user)` を呼び出す | `query.filter` が1回呼ばれること（空リストでフィルターが実行されること） |

---

## TestGenerateCsv

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 18 | 3.5.1.1 CSV生成ロジック：ヘッダー行出力（CL-4-1） | デバイスCSV仕様の全10列（操作列/デバイスID/デバイス名/デバイス種別/モデル情報/SIMID/MACアドレス/設置場所/所属組織ID/証明書期限）をキーとする dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードして1行目（ヘッダー行）を取得する | ヘッダー行に全10列名（`操作列`・`デバイスID`・`デバイス名`・`デバイス種別`・`モデル情報`・`SIMID`・`MACアドレス`・`設置場所`・`所属組織ID`・`証明書期限`）がすべて含まれること |
| 19 | 3.5.1.2 CSV生成ロジック：データ行出力（CL-4-2） | 全10列に具体的な値（デバイスID=`DEV-001`、デバイス名=`センサーA`、デバイス種別=`センサー`、モデル情報=`MODEL-A100`、SIMID=`SIM12345`、MACアドレス=`AA:BB:CC:DD:EE:FF`、設置場所=`本社1階`、所属組織ID=`1`、証明書期限=`2025-12-31`）を設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードして空行除去後の行に分割する | 出力が2行（ヘッダー行＋データ行）で構成され、データ行に `DEV-001`・`センサーA`・`センサー`・`MODEL-A100`・`SIM12345`・`AA:BB:CC:DD:EE:FF`・`本社1階`・`1`・`2025-12-31` がすべて含まれること |
| 20 | 3.5.1.2 CSV生成ロジック：データ行出力（CL-4-3: None→空文字） | 設置場所と証明書期限を `None` に設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードして空行除去後の行に分割する | 出力が2行（ヘッダー行＋データ行）で構成され、デコード結果に連続カンマ（`,,`）が含まれること（`None` フィールドが空文字として出力されること） |
| 21 | 3.5.1.3 CSV生成ロジック：0件出力 | 空リスト（`[]`）を用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードして空行除去後の行数を数える | 出力行数が0行または1行（ヘッダーのみ）であること（`len(lines) <= 1`） |
| 22 | 3.5.1.4 CSV生成ロジック：列順序 | 4列（`操作列`/`デバイスID`/`デバイス名`/`モデル情報`）の dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードしてヘッダー行をカンマ分割して列インデックスを取得する | 列順序が `操作列` < `デバイスID` < `デバイス名` < `モデル情報` の順序であること |
| 23 | 3.5.2.1 エスケープ処理：カンマエスケープ | 設置場所に `'東京,大阪'`（カンマを含む値）を設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードする | デコード結果に `"東京,大阪"` がそのまま含まれること（カンマを含む値がダブルクォートで囲まれること） |
| 24 | 3.5.2.2 エスケープ処理：改行エスケープ | 設置場所に `'東京\n大阪'`（改行を含む値）を設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードする | デコード結果にダブルクォート `"` が含まれること（pandas標準動作により改行を含む値がクォートされること） |
| 25 | 3.5.2.3 エスケープ処理：ダブルクォートエスケープ | デバイス名に `'型番"A100"センサー'`（ダブルクォートを含む値）を設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードする | デコード結果に `""` が含まれること（ダブルクォートが `""` でエスケープされること） |
| 26 | 3.5.2.4 エスケープ処理：エスケープ不要 | デバイスIDに `'DEV001'`（特殊文字なしの値）を設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードする | デコード結果に `DEV001` が不要なクォートなしでそのまま含まれること |
| 27 | 3.5.3.1 エンコーディング処理：UTF-8 BOM付き | デバイスIDを含む dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値の型を確認する | 戻り値が `bytes` 型の場合、先頭3バイトが UTF-8 BOM（`b'\xef\xbb\xbf'`）であること。`str` 型の場合は `str` 型であることのみ確認する（※要確認: str型の場合のBOM検証は実装確定後に追加予定） |
| 28 | 3.5.3.2 エンコーディング処理：日本語文字 | デバイス名に `'温度センサー１号機'`、設置場所に `'東京都渋谷区'` を設定した dict 1件のリストを用意し、`generate_csv(rows)` を呼び出す。戻り値を `_decode()` でデコードする | デコード結果に `'温度センサー１号機'` と `'東京都渋谷区'` が文字化けなく含まれること |
