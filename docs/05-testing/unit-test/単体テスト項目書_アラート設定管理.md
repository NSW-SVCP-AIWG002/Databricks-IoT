# テスト項目書 - alert-settings

- **対象ファイル（Service層）**: `src/iot_app/services/alert_setting_service.py`
- **対象ファイル（Model層）**: `src/iot_app/models/alert.py`
- **テストコード**: `tests/unit/services/test_alert_setting_service.py`, `tests/unit/models/test_alert_settings.py`
- **観点表**: `docs/05-testing/unit-test/unit-test-perspectives.md`
- **ワークフロー仕様書**: `docs/03-features/flask-app/alert-settings/workflow-specification.md`

---

## 対応表

テストクラスと、ワークフロー仕様書の各ワークフロー・処理フロー項目の対応を示します。

### test_alert_setting_service.py

| テストクラス | テスト項目No. | 対応ワークフロー | 処理フロー項目 | エンドポイント |
|------------|:---:|--------------|-------------|-------------|
| TestAlertSettingServiceSearch | 28〜33 | 初期表示、検索・絞り込み | 検索クエリ実行 | `GET /alert/alert-setting` |
| TestAlertSettingServiceValidation | 1〜27 | アラート設定登録、アラート設定更新 | バリデーション | `POST /alert/alert-setting/create`, `POST /alert/alert-setting/<alert_uuid>/update` |
| TestAlertSettingServiceCreate | 34〜38 | アラート設定登録 | INSERT処理、トランザクション管理 | `POST /alert/alert-setting/create` |
| TestAlertSettingServiceUpdate | 39〜43 | アラート設定更新 | UPDATE処理、トランザクション管理 | `POST /alert/alert-setting/<alert_uuid>/update` |
| TestAlertSettingServiceDelete | 44〜48 | アラート設定削除 | 論理削除処理、トランザクション管理 | `POST /alert/alert-setting/delete` |
| TestAlertSettingServiceGet | 49〜51 | アラート設定参照 | 詳細取得処理 | `GET /alert/alert-setting/<alert_uuid>` |
| TestAlertSettingServiceExport | 52〜56 | CSVエクスポート | CSV生成・エンコーディング | `GET /alert/alert-setting?export=csv` |
| TestAlertSettingServiceExportPermission | 57〜58 | CSVエクスポート | 権限チェック | `GET /alert/alert-setting?export=csv` |

### test_alert_settings.py

| テストクラス | テスト項目No. | 対応ワークフロー | 処理フロー項目 | エンドポイント |
|------------|:---:|--------------|-------------|-------------|
| TestAlertSettingMaster | 1〜26 | アラート設定登録、アラート設定更新、アラート設定削除、アラート設定参照 | モデル定義（カラム定義・制約） | - |
| TestAlertLevelMaster | 27〜31 | アラート設定登録、アラート設定更新 | モデル定義（アラートレベルマスタ） | - |
| TestAlertStatusMaster | 32〜35 | - | モデル定義（アラートステータスマスタ） | - |
| TestAlertHistory | 36〜42 | - | モデル定義（アラート履歴） | - |

---

## ワークフロー別カバレッジ

ワークフロー仕様書の各ワークフローに対して、どのテストクラスが対応しているかを示します。

### 初期表示

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| 検索クエリ実行（検索条件なし・全件取得） | TestAlertSettingServiceSearch | 30 |
| ページング（limit/offset計算） | TestAlertSettingServiceSearch | 31 |
| 検索結果戻り値ハンドリング | TestAlertSettingServiceSearch | 32〜33 |

### 検索・絞り込み

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| 検索クエリ実行（alert_name指定） | TestAlertSettingServiceSearch | 28 |
| 検索クエリ実行（複数条件指定） | TestAlertSettingServiceSearch | 29 |

### アラート設定登録

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| バリデーション（必須チェック） | TestAlertSettingServiceValidation | 1〜13 |
| バリデーション（最大文字列長チェック） | TestAlertSettingServiceValidation | 14〜16 |
| バリデーション（数値範囲チェック） | TestAlertSettingServiceValidation | 17〜23 |
| バリデーション（不整値チェック：判定時間） | TestAlertSettingServiceValidation | 24〜27 |
| INSERT処理（db.session.add / commit） | TestAlertSettingServiceCreate | 34〜35 |
| バリデーションエラー時の副作用なし | TestAlertSettingServiceCreate | 37 |
| トランザクションロールバック（DB例外時） | TestAlertSettingServiceCreate | 36 |
| エラー伝播 | TestAlertSettingServiceCreate | 38 |
| モデル定義（alert_setting_master） | TestAlertSettingMaster | 1〜26 |

### アラート設定更新

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| バリデーション（必須・長さ・範囲・不整値） | TestAlertSettingServiceValidation | 1〜27 |
| UPDATE処理（db.session.commit） | TestAlertSettingServiceUpdate | 39〜41 |
| 存在しないalert_uuid指定時のエラー | TestAlertSettingServiceUpdate | 42 |
| トランザクションロールバック（DB例外時） | TestAlertSettingServiceUpdate | 43 |

### アラート設定削除

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| 論理削除処理（delete_flag=True更新） | TestAlertSettingServiceDelete | 44〜46 |
| 0件更新時のエラー（存在しないUUID） | TestAlertSettingServiceDelete | 47 |
| トランザクションロールバック（DB例外時） | TestAlertSettingServiceDelete | 48 |

### アラート設定参照

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| 詳細取得処理（存在するalert_uuid） | TestAlertSettingServiceGet | 49 |
| 存在しないalert_uuid指定時のエラー | TestAlertSettingServiceGet | 50 |
| 論理削除済みデータの除外フィルタ確認 | TestAlertSettingServiceGet | 51 |

### CSVエクスポート

| 処理フロー項目 | 対応テストクラス | テスト項目No. |
|-------------|--------------|:---:|
| 権限チェック（サービス利用者は403） | TestAlertSettingServiceExportPermission | 57 |
| 権限チェック（システム保守者は正常処理） | TestAlertSettingServiceExportPermission | 58 |
| CSV生成（ヘッダー行） | TestAlertSettingServiceExport | 53 |
| CSV生成（データ行・複数件） | TestAlertSettingServiceExport | 52 |
| CSV生成（データ0件：ヘッダーのみ） | TestAlertSettingServiceExport | 54 |
| エンコーディング（UTF-8 BOM付き） | TestAlertSettingServiceExport | 55 |
| エンコーディング（日本語文字化けなし） | TestAlertSettingServiceExport | 56 |

---

## テストクラス一覧

テストクラスに対して、テスト観点、操作手順、予想結果を示します。

---

### TestAlertSettingServiceSearch

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.search()`

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 28 | 3.1.1.1 検索条件指定（条件を指定） | `mock_session.query` のチェーン（filter / order_by / limit / offset / all）を MagicMock で設定し、`all()` の返却値を空リストとする。`service.search(alert_name="温度", user_id=1)` を呼び出す | `mock_q.filter` の call_args リスト文字列表現に `"温度"` が含まれる |
| 29 | 3.1.1.2 検索条件指定（複数条件を指定） | `mock_session.query` のチェーンを MagicMock で設定し、`service.search(alert_name="温度", device_name="Device01", user_id=1)` を呼び出す | `mock_q.filter` の call_args に `"温度"` と `"Device01"` がどちらも含まれる |
| 30 | 3.1.2.1 検索条件未指定（全件相当） | `mock_session.query` のチェーンを MagicMock で設定し、`service.search(user_id=1)` を呼び出す（alert_name / device_name を指定しない） | `mock_q.filter` の call_args 文字列表現に `"alert_name"` も `"device_name"` も含まれない |
| 31 | 3.1.3.1 ページング・件数制御 | `mock_session.query` のチェーンを MagicMock で設定し、`service.search(user_id=1, page=2, per_page=25)` を呼び出す | `mock_q.limit(25)` が呼び出され、`mock_q.offset(25)` が呼び出される（offset = (2-1) × 25 = 25） |
| 32 | 3.1.4.1 検索結果戻り値ハンドリング（リスト返却） | 2件のアラート設定モック（alert_id=1, alert_id=2）を生成し、`mock_q.all()` の返却値として設定する。`service.search(user_id=1)` を呼び出す | 返却値が `mock_q.all()` の返却オブジェクト `expected` と同一（`result is expected`） |
| 33 | 3.1.4.2 検索結果戻り値ハンドリング（空結果） | `mock_q.all()` の返却値を空リスト `[]` に設定し、`service.search(user_id=1)` を呼び出す | 返却値が空リスト `[]` である |

---

### TestAlertSettingServiceValidation

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.create()` のバリデーション部

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 1.1.1 必須チェック（未入力：空文字） | `make_valid_form_data(alert_name="")` で `alert_name` を空文字にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 2 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_name=None)` で `alert_name` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 3 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(device_id=None)` で `device_id` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 4 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_conditions_measurement_item_id=None)` で対象フィールドを `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 5 | 1.1.1 必須チェック（未入力：空文字） | `make_valid_form_data(alert_conditions_operator="")` で `alert_conditions_operator` を空文字にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 6 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_conditions_operator=None)` で `alert_conditions_operator` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 7 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_conditions_threshold=None)` で `alert_conditions_threshold` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 8 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_recovery_conditions_measurement_item_id=None)` で対象フィールドを `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 9 | 1.1.1 必須チェック（未入力：空文字） | `make_valid_form_data(alert_recovery_conditions_operator="")` で `alert_recovery_conditions_operator` を空文字にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 10 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_recovery_conditions_operator=None)` で `alert_recovery_conditions_operator` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 11 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_recovery_conditions_threshold=None)` で `alert_recovery_conditions_threshold` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 12 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(judgment_time=None)` で `judgment_time` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 13 | 1.1.2 必須チェック（未入力：None） | `make_valid_form_data(alert_level_id=None)` で `alert_level_id` を `None` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 14 | 1.2.1 最大文字列長チェック（最大長-1） | `make_valid_form_data(alert_name="あ" * 99)` で `alert_name` を99文字にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされない（正常終了する） |
| 15 | 1.2.2 最大文字列長チェック（最大長） | `make_valid_form_data(alert_name="あ" * 100)` で `alert_name` を100文字にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされない（正常終了する） |
| 16 | 1.2.3 最大文字列長チェック（最大長+1） | `make_valid_form_data(alert_name="あ" * 101)` で `alert_name` を101文字にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 17 | 1.3.1 数値範囲チェック（最小値-1） | `make_valid_form_data(alert_conditions_threshold=-1_000_000_000_000_000)` で閾値を最小値未満（-1,000,000,000,000,000）にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 18 | 1.3.2 数値範囲チェック（最小値） | `make_valid_form_data(alert_conditions_threshold=-999_999_999_999_999)` で閾値を最小値ちょうど（-999,999,999,999,999）にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされない（正常終了する） |
| 19 | 1.3.3 数値範囲チェック（最大値） | `make_valid_form_data(alert_conditions_threshold=999_999_999_999_999)` で閾値を最大値ちょうど（999,999,999,999,999）にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされない（正常終了する） |
| 20 | 1.3.4 数値範囲チェック（最大値+1） | `make_valid_form_data(alert_conditions_threshold=1_000_000_000_000_000)` で閾値を最大値超過（1,000,000,000,000,000）にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 21 | 1.3.5 数値範囲チェック（数値以外） | `make_valid_form_data(alert_conditions_threshold="abc")` で `alert_conditions_threshold` に文字列を渡したデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 22 | 1.3.1 数値範囲チェック（最小値-1） | `make_valid_form_data(alert_recovery_conditions_threshold=-1_000_000_000_000_000)` で復旧条件閾値を最小値未満にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 23 | 1.3.4 数値範囲チェック（最大値+1） | `make_valid_form_data(alert_recovery_conditions_threshold=1_000_000_000_000_000)` で復旧条件閾値を最大値超過にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 24 | 1.6.1 不整値チェック（許容値） | `make_valid_form_data(judgment_time=1)` で `judgment_time` を許容値 `1` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされない（正常終了する） |
| 25 | 1.6.1 不整値チェック（許容値） | `make_valid_form_data(judgment_time=60)` で `judgment_time` を許容値 `60` にしたデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされない（正常終了する） |
| 26 | 1.6.2 不整値チェック（未定義値） | `make_valid_form_data(judgment_time=7)` で `judgment_time` に許容値外の `7` を設定したデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |
| 27 | 1.6.2 不整値チェック（未定義値） | `make_valid_form_data(judgment_time=0)` で `judgment_time` に許容されていない `0` を設定したデータを生成し、`service.create(data)` を呼び出す | `Exception` がスローされる |

---

### TestAlertSettingServiceCreate

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.create()`

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 34 | 3.2.1.1 登録処理呼び出し（正常な入力値） / 2.1.1 正常系処理 | `make_valid_form_data()` で全必須項目を含む有効データを生成し、`mock_session.add` と `mock_session.commit` の返却値を `None` に設定する。`service.create(data)` を呼び出す | `mock_session.add` が1回呼び出される。`mock_session.commit` が1回呼び出される |
| 35 | 2.1.1 正常系処理（通知フラグ False） | `make_valid_form_data(alert_notification_flag=False, alert_email_flag=False)` でデータを生成し、`mock_session.add` と `mock_session.commit` の返却値を `None` に設定する。`service.create(data)` を呼び出す | `mock_session.add` が1回呼び出される（例外が発生しない） |
| 36 | 2.3.2 副作用チェック（例外発生時のrollback） | `make_valid_form_data()` で有効データを生成し、`mock_session.commit.side_effect = Exception("DB error")` を設定する。`service.create(data)` を呼び出す | `Exception` がスローされる。`mock_session.rollback` が1回呼び出される |
| 37 | 2.3.1 副作用チェック（バリデーションエラー時のデータ変更なし） | `make_valid_form_data(alert_name="")` でバリデーションエラーが発生するデータを生成し、`service.create(data)` を try ブロックで呼び出す | `mock_session.add` が呼び出されない |
| 38 | 1.3.1 エラーハンドリング（例外伝播） | `make_valid_form_data()` で有効データを生成し、`mock_session.commit.side_effect = Exception("DB connection failed")` を設定する。`service.create(data)` を呼び出す | `Exception` がスローされ、握りつぶされずに上位へ伝播される |

---

### TestAlertSettingServiceUpdate

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.update()`

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 39 | 3.3.1.1 更新処理呼び出し（正常な入力値） / 2.1.1 正常系処理 | `alert_uuid = "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"` を用意し、`make_valid_form_data()` で有効データを生成する。`mock_session.query().filter().first()` の返却値に `make_alert_setting_mock(alert_uuid=alert_uuid)` を設定する。`service.update(alert_uuid=alert_uuid, data=data, user_id=1)` を呼び出す | `mock_session.commit` が1回呼び出される |
| 40 | 3.3.2.1 更新結果（処理完了） | `alert_uuid` を用意し、`make_valid_form_data()` で有効データを生成する。`mock_session.query().filter().first()` にモックアラートを設定する。`service.update(alert_uuid=alert_uuid, data=data, user_id=1)` を呼び出す | `Exception` がスローされない（例外なく処理が完了する） |
| 41 | 3.3.2.2 更新結果（ID指定） | `target_uuid = "target-uuid-..."` を用意し、`mock_session.query().filter().first()` にそのUUIDを持つモックアラートを設定する。`service.update(alert_uuid=target_uuid, data=data, user_id=1)` を呼び出す | `mock_session.query().filter` が1回呼び出され、filterのcall_argsに `target_uuid` が含まれる |
| 42 | 2.2.2 対象データ存在チェック（対象IDが存在しない） | `mock_session.query().filter().first()` の返却値を `None` に設定し、`make_valid_form_data()` で有効データを生成する。`service.update(alert_uuid="nonexistent-uuid", data=data, user_id=1)` を呼び出す | `Exception` がスローされる |
| 43 | 2.3.2 副作用チェック（例外発生時のrollback） | モックアラートを `first()` に設定し、`mock_session.commit.side_effect = Exception("DB error")` を設定する。`service.update(alert_uuid=alert_uuid, data=data, user_id=1)` を呼び出す | `Exception` がスローされる。`mock_session.rollback` が1回呼び出される |

---

### TestAlertSettingServiceDelete

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.delete()`

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 44 | 3.4.1.1 削除処理呼び出し（正常なIDを指定） | `uuids = ["uuid-0001", "uuid-0002"]` を用意し、`mock_session.query` のチェーン（filter / update）を MagicMock で設定する（`update` の返却値を `2`）。`service.delete(alert_uuids=uuids, user_id=1)` を呼び出す | `mock_q.filter` が1回呼び出され、filterのcall_argsに `"uuid-0001"` と `"uuid-0002"` が含まれる。`mock_session.commit` が1回呼び出される |
| 45 | 3.4.2.1 削除結果（処理完了） | `uuids = ["uuid-0001"]` を用意し、`mock_q.update` の返却値を `1` に設定する。`service.delete(alert_uuids=uuids, user_id=1)` を呼び出す | `Exception` がスローされない（例外なく処理が完了する） |
| 46 | 3.4.2.2 削除結果（delete_flag=True） | `uuids = ["uuid-0001"]` を用意し、`mock_q.update` の返却値を `1` に設定する。`service.delete(alert_uuids=uuids, user_id=1)` を呼び出す | `mock_q.update` が呼び出され、その第1位置引数の辞書に `{"delete_flag": True}` が含まれる |
| 47 | 2.2.2 対象データ存在チェック（対象IDが存在しない） | `mock_q.update` の返却値を `0`（0件更新）に設定し、`service.delete(alert_uuids=["nonexistent-uuid"], user_id=1)` を呼び出す | `Exception` がスローされる |
| 48 | 2.3.2 副作用チェック（例外発生時のrollback） | `mock_session.query` のチェーンを設定し、`mock_session.commit.side_effect = Exception("DB error")` を設定する。`service.delete(alert_uuids=["uuid-0001"], user_id=1)` を呼び出す | `Exception` がスローされる。`mock_session.rollback` が1回呼び出される |

---

### TestAlertSettingServiceGet

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.get()`

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 49 | 2.2.1 対象データ存在チェック（対象IDが存在する） / 3.1.4.1 検索結果戻り値ハンドリング | `alert_uuid = "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"` を用意し、`make_alert_setting_mock(alert_uuid=alert_uuid)` をモックとして生成する。`mock_session.query().filter().first()` の返却値に当該モックを設定する。`service.get(alert_uuid=alert_uuid, user_id=1)` を呼び出す | 返却値が `make_alert_setting_mock` で生成した `expected` オブジェクトと等しい |
| 50 | 2.2.2 対象データ存在チェック（対象IDが存在しない） | `mock_session.query().filter().first()` の返却値を `None` に設定し、`service.get(alert_uuid="nonexistent-uuid", user_id=1)` を呼び出す | `Exception` がスローされる |
| 51 | 2.2.3 対象データ存在チェック（論理削除済みデータ） | `mock_session.query` のチェーン（filter / first）を MagicMock で設定し、`first()` の返却値を `None` とする。`service.get(alert_uuid="deleted-uuid", user_id=1)` を try ブロックで呼び出す | `mock_q.filter` の call_args リスト文字列表現に `"delete_flag"` と `"False"` が含まれる（論理削除済みを除外するフィルタが適用される） |

---

### TestAlertSettingServiceExport

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.export_csv()`

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 52 | 3.5.1.2 CSV生成ロジック（データ行出力） | `mock_session.query` のチェーンを設定し、`mock_q.all()` の返却値として alert_id=1（alert_name="温度異常アラート"）と alert_id=2（alert_name="湿度異常アラート"）の2件のモックを設定する。`service.export_csv(user_id=1)` を呼び出す | 返却値の文字列に `"温度異常アラート"` と `"湿度異常アラート"` がどちらも含まれる |
| 53 | 3.5.1.1 CSV生成ロジック（ヘッダー行出力） | `mock_q.all()` の返却値を `_make_csv_data()` で生成した1件のモックに設定する。`service.export_csv(user_id=1)` を呼び出す | 返却値の文字列に以下のヘッダーが含まれる：`"アラートID"`, `"アラート名"`, `"デバイス名"`, `"アラート発生条件_測定項目名"`, `"アラート発生条件_比較演算子"`, `"アラート発生条件_閾値"`, `"アラート復旧条件_測定項目名"`, `"アラート復旧条件_比較演算子"`, `"アラート復旧条件_閾値"`, `"判定時間（分）"`, `"アラートレベル"`, `"アラート通知"`, `"メール送信"`, `"作成日時"`, `"作成者"`, `"更新日時"`, `"更新者"` |
| 54 | 3.5.1.3 CSV生成ロジック（0件出力） | `mock_q.all()` の返却値を空リスト `[]` に設定する。`service.export_csv(user_id=1)` を呼び出す | 返却値が `None` でない。返却値を改行で分割すると行数が1（ヘッダー行のみ） |
| 55 | 3.5.3.1 エンコーディング処理（UTF-8 BOM付き） | `mock_q.all()` の返却値を `_make_csv_data()` で生成した1件のモックに設定する。`service.export_csv(user_id=1)` を呼び出す | 返却値が `bytes` 型の場合は先頭3バイトが `b"\xef\xbb\xbf"`（UTF-8 BOM）である。`str` 型の場合は先頭文字が `"﻿"` で始まる |
| 56 | 3.5.3.2 エンコーディング処理（日本語文字） | `mock_q.all()` の返却値を `make_alert_setting_mock(alert_name="温度異常アラート日本語テスト")` に設定する。`service.export_csv(user_id=1)` を呼び出す | 返却値の文字列に `"温度異常アラート日本語テスト"` が文字化けなく含まれる |

---

### TestAlertSettingServiceExportPermission

**テストファイル**: `tests/unit/services/test_alert_setting_service.py`
**対象メソッド**: `AlertSettingService.export_csv()` の権限チェック部

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 57 | 1.2.2 認可（権限チェック）機能（権限なし） | `user_type_id=4`（サービス利用者）を持つモックユーザーを生成し、`mock_session.query().filter().first()` の返却値に設定する。`service.export_csv(user_id=1)` を呼び出す | `Exception` がスローされる（ForbiddenError相当） |
| 58 | 1.2.1 認可（権限チェック）機能（権限あり） | `user_type_id=1`（システム保守者）を持つモックユーザーを生成し、`mock_session.query().filter().first()` の返却値に設定する。`service.export_csv(user_id=1)` を呼び出す | `Exception` がスローされない（正常に処理される） |

---

### TestAlertSettingMaster

**テストファイル**: `tests/unit/models/test_alert_settings.py`
**対象クラス**: `AlertSettingMaster`（`src/iot_app/models/alert.py`）

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | モデル定義（テーブル名） | `from iot_app.models.alert import AlertSettingMaster` をインポートし、`AlertSettingMaster.__tablename__` を参照する | 値が `"alert_setting_master"` である |
| 2 | モデル定義（主キー） | `AlertSettingMaster.__table__.columns["alert_id"]` を取得し、`.primary_key` を参照する | `col.primary_key` が `True` である |
| 3 | モデル定義（AUTO_INCREMENT） | `AlertSettingMaster.__table__.columns["alert_id"]` を取得し、`.autoincrement` を参照する | `col.autoincrement` が `True` または `"auto"` である |
| 4 | モデル定義（カラム存在確認） | `AlertSettingMaster` に `hasattr(AlertSettingMaster, "alert_uuid")` を評価する | `True` が返る（`alert_uuid` 属性が存在する） |
| 5 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_uuid"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 6 | 1.1.2 最大文字列長チェック（alert_name最大100文字） | `AlertSettingMaster.__table__.columns["alert_name"]` を取得し、`.type.length` を参照する | `col.type.length` が `100` である |
| 7 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_name"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 8 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["device_id"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 9 | モデル定義（外部キー制約） | `AlertSettingMaster.__table__.columns["device_id"]` を取得し、`.foreign_keys` の `target_fullname` セットを確認する | `"device_master.device_id"` が外部キーターゲットに含まれる |
| 10 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_conditions_measurement_item_id"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 11 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_conditions_operator"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 12 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_conditions_threshold"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 13 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_recovery_conditions_measurement_item_id"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 14 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_recovery_conditions_operator"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 15 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["alert_recovery_conditions_threshold"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 16 | モデル定義（デフォルト値） | `AlertSettingMaster.__table__.columns["judgment_time"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `5` である |
| 17 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["judgment_time"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 18 | モデル定義（外部キー制約） | `AlertSettingMaster.__table__.columns["alert_level_id"]` を取得し、`.foreign_keys` の `target_fullname` セットを確認する | `"alert_level_master.alert_level_id"` が外部キーターゲットに含まれる |
| 19 | モデル定義（デフォルト値） | `AlertSettingMaster.__table__.columns["alert_notification_flag"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `True` である |
| 20 | モデル定義（デフォルト値） | `AlertSettingMaster.__table__.columns["alert_email_flag"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `True` である |
| 21 | モデル定義（デフォルト値） | `AlertSettingMaster.__table__.columns["delete_flag"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `False` である |
| 22 | モデル定義（NOT NULL制約） | `AlertSettingMaster.__table__.columns["delete_flag"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 23 | モデル定義（NOT NULL制約・監査証跡） | `AlertSettingMaster.__table__.columns["create_date"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 24 | モデル定義（NOT NULL制約・監査証跡） | `AlertSettingMaster.__table__.columns["creator"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 25 | モデル定義（NOT NULL制約・監査証跡） | `AlertSettingMaster.__table__.columns["update_date"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 26 | モデル定義（NOT NULL制約・監査証跡） | `AlertSettingMaster.__table__.columns["modifier"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |

---

### TestAlertLevelMaster

**テストファイル**: `tests/unit/models/test_alert_settings.py`
**対象クラス**: `AlertLevelMaster`（`src/iot_app/models/alert.py`）

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 27 | モデル定義（テーブル名） | `from iot_app.models.alert import AlertLevelMaster` をインポートし、`AlertLevelMaster.__tablename__` を参照する | 値が `"alert_level_master"` である |
| 28 | モデル定義（主キー） | `AlertLevelMaster.__table__.columns["alert_level_id"]` を取得し、`.primary_key` を参照する | `col.primary_key` が `True` である |
| 29 | 1.1.2 最大文字列長チェック（alert_level_name最大100文字） | `AlertLevelMaster.__table__.columns["alert_level_name"]` を取得し、`.type.length` を参照する | `col.type.length` が `100` である |
| 30 | モデル定義（NOT NULL制約） | `AlertLevelMaster.__table__.columns["alert_level_name"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 31 | モデル定義（デフォルト値） | `AlertLevelMaster.__table__.columns["delete_flag"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `False` である |

---

### TestAlertStatusMaster

**テストファイル**: `tests/unit/models/test_alert_settings.py`
**対象クラス**: `AlertStatusMaster`（`src/iot_app/models/alert.py`）

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 32 | モデル定義（テーブル名） | `from iot_app.models.alert import AlertStatusMaster` をインポートし、`AlertStatusMaster.__tablename__` を参照する | 値が `"alert_status_master"` である |
| 33 | モデル定義（主キー） | `AlertStatusMaster.__table__.columns["alert_status_id"]` を取得し、`.primary_key` を参照する | `col.primary_key` が `True` である |
| 34 | モデル定義（NOT NULL制約） | `AlertStatusMaster.__table__.columns["alert_status_name"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 35 | モデル定義（デフォルト値） | `AlertStatusMaster.__table__.columns["delete_flag"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `False` である |

---

### TestAlertHistory

**テストファイル**: `tests/unit/models/test_alert_settings.py`
**対象クラス**: `AlertHistory`（`src/iot_app/models/alert.py`）

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 36 | モデル定義（テーブル名） | `from iot_app.models.alert import AlertHistory` をインポートし、`AlertHistory.__tablename__` を参照する | 値が `"alert_history"` である |
| 37 | モデル定義（主キー） | `AlertHistory.__table__.columns["alert_history_id"]` を取得し、`.primary_key` を参照する | `col.primary_key` が `True` である |
| 38 | モデル定義（外部キー制約） | `AlertHistory.__table__.columns["alert_id"]` を取得し、`.foreign_keys` の `target_fullname` セットを確認する | `"alert_setting_master.alert_id"` が外部キーターゲットに含まれる |
| 39 | モデル定義（NULL許容） | `AlertHistory.__table__.columns["alert_recovery_datetime"]` を取得し、`.nullable` を参照する | `col.nullable` が `True` である（復旧前はNULL許容） |
| 40 | モデル定義（NULL許容） | `AlertHistory.__table__.columns["alert_value"]` を取得し、`.nullable` を参照する | `col.nullable` が `True` である |
| 41 | モデル定義（NOT NULL制約） | `AlertHistory.__table__.columns["alert_occurrence_datetime"]` を取得し、`.nullable` を参照する | `col.nullable` が `False` である |
| 42 | モデル定義（デフォルト値） | `AlertHistory.__table__.columns["delete_flag"]` を取得し、`.default.arg` を参照する | `col.default.arg` が `False` である |
