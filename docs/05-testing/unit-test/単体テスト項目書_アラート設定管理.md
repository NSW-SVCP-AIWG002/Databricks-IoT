# テスト項目書 アラート設定管理

## テスト対象

| 区分 | ファイルパス |
|------|------------|
| Service層 | tests/unit/services/test_alert_setting_service.py |
| Model層 | tests/unit/models/test_alert_settings.py |

---

## Service層テスト（test_alert_setting_service.py）

### TestAlertSettingServiceValidation

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 1.1.1 必須チェック：空文字 | `make_valid_form_data(alert_name="")` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 2 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_name=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 3 | 1.1.2 必須チェック：None | `make_valid_form_data(device_id=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 4 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_conditions_measurement_item_id=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 5 | 1.1.1 必須チェック：空文字 | `make_valid_form_data(alert_conditions_operator="")` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 6 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_conditions_operator=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 7 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_conditions_threshold=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 8 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_recovery_conditions_measurement_item_id=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 9 | 1.1.1 必須チェック：空文字 | `make_valid_form_data(alert_recovery_conditions_operator="")` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 10 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_recovery_conditions_operator=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 11 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_recovery_conditions_threshold=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 12 | 1.1.2 必須チェック：None | `make_valid_form_data(judgment_time=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 13 | 1.1.2 必須チェック：None | `make_valid_form_data(alert_level_id=None)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 14 | 1.2.1 最大文字列長チェック：最大長-1文字 | `make_valid_form_data(alert_name="あ"×99)` で99文字の入力データを作成し、`service.create(data)` を呼び出す | 例外が発生しない |
| 15 | 1.2.2 最大文字列長チェック：最大長ちょうど | `make_valid_form_data(alert_name="あ"×100)` で100文字の入力データを作成し、`service.create(data)` を呼び出す | 例外が発生しない |
| 16 | 1.2.3 最大文字列長チェック：最大長+1文字 | `make_valid_form_data(alert_name="あ"×101)` で101文字の入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 17 | 1.3.1 数値範囲チェック：最小値-1 | `make_valid_form_data(alert_conditions_threshold=-1_000_000_000_000_000)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 18 | 1.3.2 数値範囲チェック：最小値ちょうど | `make_valid_form_data(alert_conditions_threshold=-999_999_999_999_999)` で入力データを作成し、`service.create(data)` を呼び出す | 例外が発生しない |
| 19 | 1.3.5 数値範囲チェック：最大値ちょうど | `make_valid_form_data(alert_conditions_threshold=999_999_999_999_999)` で入力データを作成し、`service.create(data)` を呼び出す | 例外が発生しない |
| 20 | 1.3.6 数値範囲チェック：最大値+1 | `make_valid_form_data(alert_conditions_threshold=1_000_000_000_000_000)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 21 | 1.3.7 数値範囲チェック：数値以外 | `make_valid_form_data(alert_conditions_threshold="abc")` で文字列を設定した入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 22 | 1.3.1 数値範囲チェック：最小値-1 | `make_valid_form_data(alert_recovery_conditions_threshold=-1_000_000_000_000_000)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 23 | 1.3.6 数値範囲チェック：最大値+1 | `make_valid_form_data(alert_recovery_conditions_threshold=1_000_000_000_000_000)` で入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 24 | 1.6.1 不整値チェック：許容値 | `make_valid_form_data(judgment_time=1)` で入力データを作成し、`service.create(data)` を呼び出す | 例外が発生しない |
| 25 | 1.6.1 不整値チェック：許容値 | `make_valid_form_data(judgment_time=60)` で入力データを作成し、`service.create(data)` を呼び出す | 例外が発生しない |
| 26 | 1.6.2 不整値チェック：未定義値 | `make_valid_form_data(judgment_time=7)` で許容値以外の値を設定した入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |
| 27 | 1.6.2 不整値チェック：未定義値 | `make_valid_form_data(judgment_time=0)` で許容値以外の値を設定した入力データを作成し、`service.create(data)` を呼び出す | Exception がスローされる |

---

### TestAlertSettingServiceSearch

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 3.1.1.1 検索条件指定：条件指定 | DBセッションのクエリビルダをモック化し `all()` の戻り値に空リストを設定する。`service.search(alert_name="温度", user_id=1)` を呼び出す | `filter` の呼び出し引数文字列に `"温度"` が含まれる |
| 2 | 3.1.1.2 検索条件指定：複数条件指定 | DBセッションのクエリビルダをモック化し `all()` の戻り値に空リストを設定する。`service.search(alert_name="温度", device_name="Device01", user_id=1)` を呼び出す | `filter` の呼び出し引数文字列に `"温度"` および `"Device01"` の両方が含まれる |
| 3 | 3.1.2.1 検索条件未指定（全件相当）：条件なし | DBセッションのクエリビルダをモック化し `all()` の戻り値に空リストを設定する。`service.search(user_id=1)`（`alert_name`、`device_name` 未指定）を呼び出す | `filter` の呼び出し引数文字列に `"alert_name"` および `"device_name"` が含まれない |
| 4 | 3.1.3.1 ページング・件数制御：設定値あり | DBセッションのクエリビルダをモック化する。`service.search(user_id=1, page=2, per_page=25)` を呼び出す | `mock_q.limit(25)` が1回呼ばれ、`mock_q.offset(25)`（(2-1)×25=25）が1回呼ばれる |
| 5 | 3.1.4.1 検索結果戻り値ハンドリング：正常系 | `make_alert_setting_mock()` でアラート設定モック2件のリストを作成し、クエリビルダの `all()` 戻り値に設定する。`service.search(user_id=1)` を呼び出す | 戻り値がモックで設定したリストオブジェクトと同一（`is` で一致）である |
| 6 | 3.1.4.2 検索結果戻り値ハンドリング：空結果 | クエリビルダのモックを設定し `all()` の戻り値に空リストを設定する。`service.search(user_id=1)` を呼び出す | 戻り値が空リスト `[]` である |

---

### TestAlertSettingServiceCreate

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 3.2.1.1 登録処理呼び出し / 2.1.1 正常系処理：有効な入力データ | `make_valid_form_data()` で全必須項目を含む有効なデータを作成する。`mock_session.add` / `commit` の戻り値を None に設定し、`service.create(data)` を呼び出す | `mock_session.add()` が1回呼ばれ、`mock_session.commit()` が1回呼ばれる |
| 2 | 2.1.1 正常系処理：有効な入力データ | `make_valid_form_data(alert_notification_flag=False, alert_email_flag=False)` で入力データを作成し、`service.create(data)` を呼び出す | `mock_session.add()` が1回呼ばれる（例外が発生しない） |
| 3 | 2.3.2 副作用チェック：例外発生時rollback | `make_valid_form_data()` で有効なデータを作成し、`mock_session.commit.side_effect = Exception("DB error")` を設定して `service.create(data)` を呼び出す | Exception がスローされ、`mock_session.rollback()` が1回呼ばれる |
| 4 | 2.3.1 副作用チェック：処理失敗時データ更新なし | `make_valid_form_data(alert_name="")` でバリデーションエラーとなる入力データを作成し、`service.create(data)` を呼び出す（例外は握りつぶす） | `mock_session.add()` が呼ばれない |
| 5 | 1.3.1 エラーハンドリング：例外伝播 | `make_valid_form_data()` で有効なデータを作成し、`mock_session.commit.side_effect = Exception("DB connection failed")` を設定して `service.create(data)` を呼び出す | Exception が握りつぶされず上位へ伝播する（`pytest.raises` で捕捉できる） |

---

### TestAlertSettingServiceUpdate

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 3.3.1.1 更新処理呼び出し / 2.1.1 正常系処理：有効な入力データ | `alert_uuid="aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"` のアラート設定モックをクエリチェーンの `first()` に設定する。`make_valid_form_data()` で有効なデータを作成し、`service.update(alert_uuid=..., data=data, user_id=1)` を呼び出す | `mock_session.commit()` が1回呼ばれる |
| 2 | 3.3.2.1 更新結果：処理完了 | `alert_uuid="aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"` のアラート設定モックをクエリチェーンの `first()` に設定する。`make_valid_form_data()` で有効なデータを作成し、`service.update(alert_uuid=..., data=data, user_id=1)` を呼び出す | 例外が発生しない |
| 3 | 3.3.2.2 更新結果：ID指定 | `target_uuid="target-uuid-0001-0001-0001-000000000001"` のアラート設定モックをクエリチェーンの `first()` に設定する。`service.update(alert_uuid=target_uuid, data=data, user_id=1)` を呼び出す | `mock_session.query().filter()` が1回呼ばれ、その呼び出し引数に `target_uuid` の値が含まれる |
| 4 | 2.2.2 対象データ存在チェック：対象IDが存在しない | クエリチェーンの `first()` 戻り値を None に設定する。`make_valid_form_data()` で有効なデータを作成し、`service.update(alert_uuid="nonexistent-uuid", data=data, user_id=1)` を呼び出す | Exception がスローされる |
| 5 | 2.3.2 副作用チェック：例外発生時rollback | アラート設定モックをクエリチェーンの `first()` に設定し、`mock_session.commit.side_effect = Exception("DB error")` を設定する。`service.update(alert_uuid=..., data=data, user_id=1)` を呼び出す | Exception がスローされ、`mock_session.rollback()` が1回呼ばれる |

---

### TestAlertSettingServiceDelete

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 3.4.1.1 削除処理呼び出し | クエリビルダのモックを設定し `update()` の戻り値を2に設定する。`service.delete(alert_uuids=["uuid-0001", "uuid-0002"], user_id=1)` を呼び出す | `mock_q.filter()` が1回呼ばれ、その引数に `"uuid-0001"` および `"uuid-0002"` が含まれ、`mock_session.commit()` が1回呼ばれる |
| 2 | 3.4.2.1 削除結果：処理完了 | クエリビルダのモックを設定し `update()` の戻り値を1に設定する。`service.delete(alert_uuids=["uuid-0001"], user_id=1)` を呼び出す | 例外が発生しない |
| 3 | 3.4.2.2 削除結果：ID指定（論理削除） | クエリビルダのモックを設定し `update()` の戻り値を1に設定する。`service.delete(alert_uuids=["uuid-0001"], user_id=1)` を呼び出す | `mock_q.update()` が呼ばれ、その位置引数の辞書に `delete_flag=True` が含まれる |
| 4 | 2.2.2 対象データ存在チェック：対象IDが存在しない | クエリビルダのモックを設定し `update()` の戻り値を0（0件更新）に設定する。`service.delete(alert_uuids=["nonexistent-uuid"], user_id=1)` を呼び出す | Exception がスローされる |
| 5 | 2.3.2 副作用チェック：例外発生時rollback | クエリビルダのモックを設定し、`mock_session.commit.side_effect = Exception("DB error")` を設定する。`service.delete(alert_uuids=["uuid-0001"], user_id=1)` を呼び出す | Exception がスローされ、`mock_session.rollback()` が1回呼ばれる |

---

### TestAlertSettingServiceGet

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 2.2.1 対象データ存在チェック：対象IDが存在する / 3.1.4.1 検索結果戻り値ハンドリング：正常系 | `alert_uuid="aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"` のアラート設定モックを作成し、クエリチェーンの `first()` に設定する。`service.get(alert_uuid=..., user_id=1)` を呼び出す | 戻り値がモックで設定したアラート設定オブジェクトと等しい（`==` で一致） |
| 2 | 2.2.2 対象データ存在チェック：対象IDが存在しない | クエリチェーンの `first()` 戻り値を None に設定する。`service.get(alert_uuid="nonexistent-uuid", user_id=1)` を呼び出す | Exception がスローされる |
| 3 | 2.2.3 対象データ存在チェック：論理削除済みデータ | クエリビルダのモックを設定し `first()` 戻り値を None とする。`service.get(alert_uuid="deleted-uuid", user_id=1)` を呼び出す（例外は握りつぶす） | `filter` の呼び出し引数文字列に `"delete_flag"` および `"False"` が含まれる（論理削除済みレコードを除外する条件が付与される） |

---

### TestAlertSettingServiceExport

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 3.5.1.2 CSV生成ロジック：データ行出力 | クエリビルダのモックを設定し、`all()` の戻り値に `alert_id=1`（"温度異常アラート"）と `alert_id=2`（"湿度異常アラート"）の2件を設定する。`service.export_csv(user_id=1)` を呼び出す | 戻り値に `"温度異常アラート"` および `"湿度異常アラート"` の両方が含まれる |
| 2 | 3.5.1.1 CSV生成ロジック：ヘッダー行出力 | クエリビルダのモックを設定し、`all()` の戻り値に1件のアラート設定データ（`_make_csv_data()`）を設定する。`service.export_csv(user_id=1)` を呼び出す | 戻り値に `"アラートID"`、`"アラート名"`、`"デバイス名"`、`"アラート発生条件_測定項目名"`、`"アラート発生条件_比較演算子"`、`"アラート発生条件_閾値"`、`"アラート復旧条件_測定項目名"`、`"アラート復旧条件_比較演算子"`、`"アラート復旧条件_閾値"`、`"判定時間（分）"`、`"アラートレベル"`、`"アラート通知"`、`"メール送信"`、`"作成日時"`、`"作成者"`、`"更新日時"`、`"更新者"` がすべて含まれる |
| 3 | 3.5.1.3 CSV生成ロジック：0件出力 | クエリビルダのモックを設定し、`all()` の戻り値に空リストを設定する。`service.export_csv(user_id=1)` を呼び出す | 戻り値が None でなく、改行で分割した結果が1行のみ（ヘッダー行のみ）である |
| 4 | 3.5.3.1 エンコーディング処理：UTF-8 BOM付き | クエリビルダのモックを設定し、`all()` の戻り値に1件のアラート設定データを設定する。`service.export_csv(user_id=1)` を呼び出す | 戻り値がバイト列の場合は先頭3バイトが `b"\xef\xbb\xbf"`（UTF-8 BOM）、文字列の場合は先頭が `"﻿"`（BOM文字）である |
| 5 | 3.5.3.2 エンコーディング処理：日本語文字 | クエリビルダのモックを設定し、`all()` の戻り値に `alert_name="温度異常アラート日本語テスト"` のアラート設定データを設定する。`service.export_csv(user_id=1)` を呼び出す | 戻り値に `"温度異常アラート日本語テスト"` が文字化けなく含まれる |

---

### TestAlertSettingServiceExportPermission

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 1.2.2 認可（権限チェック）：権限なし | `user_type_id=4`（サービス利用者）のユーザーモックをクエリチェーンの `first()` に設定する。`service.export_csv(user_id=1)` を呼び出す | Exception がスローされる（ForbiddenError） |
| 2 | 1.2.1 認可（権限チェック）：権限あり | `user_type_id=1`（システム保守者）のユーザーモックをクエリチェーンの `first()` に設定する。`service.export_csv(user_id=1)` を呼び出す | 例外が発生しない |

---

## Model層テスト（test_alert_settings.py）

### TestAlertSettingMaster

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | モデル定義チェック：テーブル名 | `AlertSettingMaster` クラスをインポートし、`__tablename__` を参照する | `__tablename__` が `"alert_setting_master"` である |
| 2 | モデル定義チェック：主キー定義 | `AlertSettingMaster.__table__.columns["alert_id"]` を取得し、`primary_key` を確認する | `col.primary_key` が `True` である |
| 3 | モデル定義チェック：自動採番 | `AlertSettingMaster.__table__.columns["alert_id"]` を取得し、`autoincrement` を確認する | `col.autoincrement` が `True` または `"auto"` である |
| 4 | モデル定義チェック：カラム存在確認 | `AlertSettingMaster` クラスをインポートし、`hasattr(AlertSettingMaster, "alert_uuid")` を確認する | `True`（`alert_uuid` 属性が存在する） |
| 5 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_uuid"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 6 | 1.1.2 最大文字列長チェック：alert_name最大100文字 | `AlertSettingMaster.__table__.columns["alert_name"]` を取得し、`type.length` を確認する | `col.type.length` が `100` である |
| 7 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_name"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 8 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["device_id"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 9 | モデル定義チェック：外部キー制約 | `AlertSettingMaster.__table__.columns["device_id"].foreign_keys` から外部キーターゲット一覧を取得する | `"device_master.device_id"` が外部キーターゲットに含まれる |
| 10 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_conditions_measurement_item_id"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 11 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_conditions_operator"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 12 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_conditions_threshold"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 13 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_recovery_conditions_measurement_item_id"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 14 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_recovery_conditions_operator"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 15 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["alert_recovery_conditions_threshold"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 16 | モデル定義チェック：デフォルト値 | `AlertSettingMaster.__table__.columns["judgment_time"]` を取得し、`default.arg` を確認する | `col.default.arg` が `5` である |
| 17 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["judgment_time"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 18 | モデル定義チェック：外部キー制約 | `AlertSettingMaster.__table__.columns["alert_level_id"].foreign_keys` から外部キーターゲット一覧を取得する | `"alert_level_master.alert_level_id"` が外部キーターゲットに含まれる |
| 19 | モデル定義チェック：デフォルト値 | `AlertSettingMaster.__table__.columns["alert_notification_flag"]` を取得し、`default.arg` を確認する | `col.default.arg` が `True` である |
| 20 | モデル定義チェック：デフォルト値 | `AlertSettingMaster.__table__.columns["alert_email_flag"]` を取得し、`default.arg` を確認する | `col.default.arg` が `True` である |
| 21 | モデル定義チェック：デフォルト値（論理削除初期値） | `AlertSettingMaster.__table__.columns["delete_flag"]` を取得し、`default.arg` を確認する | `col.default.arg` が `False` である |
| 22 | モデル定義チェック：NOT NULL制約 | `AlertSettingMaster.__table__.columns["delete_flag"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 23 | モデル定義チェック：NOT NULL制約（監査証跡） | `AlertSettingMaster.__table__.columns["create_date"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 24 | モデル定義チェック：NOT NULL制約（監査証跡） | `AlertSettingMaster.__table__.columns["creator"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 25 | モデル定義チェック：NOT NULL制約（監査証跡） | `AlertSettingMaster.__table__.columns["update_date"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 26 | モデル定義チェック：NOT NULL制約（監査証跡） | `AlertSettingMaster.__table__.columns["modifier"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |

---

### TestAlertLevelMaster

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | モデル定義チェック：テーブル名 | `AlertLevelMaster` クラスをインポートし、`__tablename__` を参照する | `__tablename__` が `"alert_level_master"` である |
| 2 | モデル定義チェック：主キー定義 | `AlertLevelMaster.__table__.columns["alert_level_id"]` を取得し、`primary_key` を確認する | `col.primary_key` が `True` である |
| 3 | 1.1.2 最大文字列長チェック：alert_level_name最大100文字 | `AlertLevelMaster.__table__.columns["alert_level_name"]` を取得し、`type.length` を確認する | `col.type.length` が `100` である |
| 4 | モデル定義チェック：NOT NULL制約 | `AlertLevelMaster.__table__.columns["alert_level_name"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 5 | モデル定義チェック：デフォルト値（論理削除初期値） | `AlertLevelMaster.__table__.columns["delete_flag"]` を取得し、`default.arg` を確認する | `col.default.arg` が `False` である |

---

### TestAlertStatusMaster

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | モデル定義チェック：テーブル名 | `AlertStatusMaster` クラスをインポートし、`__tablename__` を参照する | `__tablename__` が `"alert_status_master"` である |
| 2 | モデル定義チェック：主キー定義 | `AlertStatusMaster.__table__.columns["alert_status_id"]` を取得し、`primary_key` を確認する | `col.primary_key` が `True` である |
| 3 | モデル定義チェック：NOT NULL制約 | `AlertStatusMaster.__table__.columns["alert_status_name"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 4 | モデル定義チェック：デフォルト値（論理削除初期値） | `AlertStatusMaster.__table__.columns["delete_flag"]` を取得し、`default.arg` を確認する | `col.default.arg` が `False` である |

---

### TestAlertHistory

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | モデル定義チェック：テーブル名 | `AlertHistory` クラスをインポートし、`__tablename__` を参照する | `__tablename__` が `"alert_history"` である |
| 2 | モデル定義チェック：主キー定義 | `AlertHistory.__table__.columns["alert_history_id"]` を取得し、`primary_key` を確認する | `col.primary_key` が `True` である |
| 3 | モデル定義チェック：外部キー制約 | `AlertHistory.__table__.columns["alert_id"].foreign_keys` から外部キーターゲット一覧を取得する | `"alert_setting_master.alert_id"` が外部キーターゲットに含まれる |
| 4 | モデル定義チェック：NULL許容（復旧前はNULL） | `AlertHistory.__table__.columns["alert_recovery_datetime"]` を取得し、`nullable` を確認する | `col.nullable` が `True` である |
| 5 | モデル定義チェック：NULL許容 | `AlertHistory.__table__.columns["alert_value"]` を取得し、`nullable` を確認する | `col.nullable` が `True` である |
| 6 | モデル定義チェック：NOT NULL制約 | `AlertHistory.__table__.columns["alert_occurrence_datetime"]` を取得し、`nullable` を確認する | `col.nullable` が `False` である |
| 7 | モデル定義チェック：デフォルト値（論理削除初期値） | `AlertHistory.__table__.columns["delete_flag"]` を取得し、`default.arg` を確認する | `col.default.arg` が `False` である |
