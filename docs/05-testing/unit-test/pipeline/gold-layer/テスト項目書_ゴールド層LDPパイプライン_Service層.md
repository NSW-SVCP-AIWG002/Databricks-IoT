# テスト項目書 - ゴールド層LDPパイプライン Service層

- **対象ファイル**: `src/pipeline/gold/gold_ldp_pipeline.py`
- **テストコード**: `/workspaces/Databricks-IoT/tests/unit/pipeline/gold/test_gold-ldp-pipeline_service.py`
- **観点表**: `docs/05-testing/unit-test/unit-test-perspectives.md`
- **ワークフロー仕様書**: `/workspaces/Databricks-IoT/docs/03-features/ldp-pipeline/gold_layer/ldp-pipeline-specification.md`

---

## 対応表

テストクラスと、ワークフロー仕様書の各ワークフロー・処理フロー項目の対応を示します。

| テストクラス          | テスト項目No. | 対応ワークフロー                           | 処理フロー項目                                            | エンドポイント                            |
| --------------------- | :-----------: | ------------------------------------------ | --------------------------------------------------------- | ----------------------------------------- |
| TestRunAggregation    |     1〜17     | 時次集計処理 / 日次集計処理 / 月次集計処理 | ①シルバー層データ読込 / ②共通集計処理 / ③ゴールド層へ書込 | `run_aggregation()`                       |
| TestTeamsNotifier     |    18〜24     | エラーハンドリング（Teams通知フロー）      | Teams Webhook呼び出し（通知送信）                         | `TeamsNotifier.send_error_notification()` |
| TestBuildAdaptiveCard |    25〜28     | エラーハンドリング（Teams通知フロー）      | 通知メッセージペイロード作成                              | `TeamsNotifier._build_adaptive_card()`    |
| TestNotifyError       |      29       | エラーハンドリング（Teams通知フロー）      | Webhook URL取得・TeamsNotifier呼び出し                    | `notify_error()`                          |

---

## ワークフロー別カバレッジ

ワークフロー仕様書の各ワークフローに対して、どのテストクラスが対応しているかを示します。

### 時次集計処理

| 処理フロー項目                         | 対応テストクラス   | テスト項目No. |
| -------------------------------------- | ------------------ | :-----------: |
| ①シルバー層データ読込                  | TestRunAggregation |     1, 4      |
| ②共通集計処理（aggregate_sensor_data） | TestRunAggregation |     1, 4      |
| ③ゴールド層へ書込（merge_to_gold）     | TestRunAggregation |     1, 4      |

### 日次集計処理

| 処理フロー項目                         | 対応テストクラス   |          テスト項目No.          |
| -------------------------------------- | ------------------ | :-----------------------------: |
| ①シルバー層データ読込                  | TestRunAggregation | 2, 5, 7, 8, 9, 10, 13, 14, 15  |
| ②共通集計処理（aggregate_sensor_data） | TestRunAggregation |        2, 5, 11, 16             |
| ③ゴールド層へ書込（merge_to_gold）     | TestRunAggregation |        2, 5, 12, 17             |

### 月次集計処理

| 処理フロー項目                         | 対応テストクラス   | テスト項目No. |
| -------------------------------------- | ------------------ | :-----------: |
| ①シルバー層データ読込                  | TestRunAggregation |     3, 6      |
| ②共通集計処理（aggregate_sensor_data） | TestRunAggregation |     3, 6      |
| ③ゴールド層へ書込（merge_to_gold）     | TestRunAggregation |     3, 6      |

### エラーハンドリング（Teams通知フロー）

| 処理フロー項目                         | 対応テストクラス      | テスト項目No. |
| -------------------------------------- | --------------------- | :-----------: |
| 通知メッセージペイロード作成           | TestBuildAdaptiveCard |    25〜28     |
| Teams Webhook呼び出し（通知送信）      | TestTeamsNotifier     |    18〜24     |
| Webhook URL取得・TeamsNotifier呼び出し | TestNotifyError       |      29       |

---

## テストクラス一覧

テストクラスに対して、テスト観点、操作手順、予想結果を示します。

### TestRunAggregation

| No  | テスト観点                              | 操作手順                                                                                                                                                                                                                                                                                                                                                                                                                     | 予想結果                                                                                                                                                                                                                                       |
| --- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 2.1.1 正常処理                          | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` の戻り値を MagicMock に設定する。`run_aggregation(AggregationPeriod.HOURLY, "event_datetime = '2026-01-28 0:00:00'")` を呼び出す。                                                                                                                 | `aggregate_sensor_data` が1回呼ばれる。`merge_to_gold` が1回呼ばれる。`notify_error` は呼ばれない。                                                                                                                                            |
| 2   | 2.1.1 正常処理                          | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` の戻り値を MagicMock に設定する。`run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                                                                              | `aggregate_sensor_data` が1回呼ばれる。`merge_to_gold` が1回呼ばれる。`notify_error` は呼ばれない。                                                                                                                                            |
| 3   | 2.1.1 正常処理                          | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` の戻り値を MagicMock に設定する。`run_aggregation(AggregationPeriod.MONTHLY, "event_date BETWEEN '2026-01-01' AND '2026-01-31'")` を呼び出す。                                                                                                     | `aggregate_sensor_data` が1回呼ばれる。`merge_to_gold` が1回呼ばれる。`notify_error` は呼ばれない。                                                                                                                                            |
| 4   | 2.1.1 正常処理 / 1.4.1.3 INFOレベル出力 | `aggregate_sensor_data`・`merge_to_gold` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` の戻り値の `count()` が `100` を返すように設定する。`merge_to_gold` の戻り値を `None` に設定する。`caplog.at_level(logging.INFO)` コンテキストで `run_aggregation(AggregationPeriod.HOURLY, "event_datetime = '2026-01-28 0:00:00'")` を呼び出す。                 | caplog のレコードの中に「集計完了」を含む INFO レベルのメッセージが存在する。                                                                                                                                                                  |
| 5   | 2.1.1 正常処理 / 1.4.1.3 INFOレベル出力 | `aggregate_sensor_data`・`merge_to_gold` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` の戻り値の `count()` が `100` を返すように設定する。`merge_to_gold` の戻り値を `None` に設定する。`caplog.at_level(logging.INFO)` コンテキストで `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                              | caplog のレコードの中に「集計完了」を含む INFO レベルのメッセージが存在する。                                                                                                                                                                  |
| 6   | 2.1.1 正常処理 / 1.4.1.3 INFOレベル出力 | `aggregate_sensor_data`・`merge_to_gold` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` の戻り値の `count()` が `100` を返すように設定する。`merge_to_gold` の戻り値を `None` に設定する。`caplog.at_level(logging.INFO)` コンテキストで `run_aggregation(AggregationPeriod.MONTHLY, "event_date BETWEEN '2026-01-01' AND '2026-01-31'")` を呼び出す。     | caplog のレコードの中に「集計完了」を含む INFO レベルのメッセージが存在する。                                                                                                                                                                  |
| 7   | 1.4.1.2 WARNレベル出力                  | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `isEmpty=True` を返す MagicMock を設定する。`caplog.at_level(logging.WARNING)` コンテキストで `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                                                                                      | `merge_to_gold` は呼ばれない。`aggregate_sensor_data` は呼ばれない。`notify_error` は呼ばれない。caplog のレコードの中に `"GOLD_WARN_001: 対象期間のデータが存在しません"` を含むメッセージが存在する。                                        |
| 8   | 1.3.1 例外伝播 / 2.3.2 例外発生時       | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `AnalysisException("Table not found")` を送出するように設定する。`pytest.raises(AnalysisException)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                                                                       | `AnalysisException` が上位に伝播する。`filter` の呼び出し回数が1回（リトライなし）。`notify_error` が1回呼ばれ、キーワード引数 `error_code` が `"GOLD_ERR_001"` である。`aggregate_sensor_data` は呼ばれない。`merge_to_gold` は呼ばれない。   |
| 9   | 1.3.1 例外伝播 / 2.3.2 例外発生時       | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `PermissionError("Access denied")` を送出するように設定する。`pytest.raises(PermissionError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                                                                             | `PermissionError` が上位に伝播する。`filter` の呼び出し回数が1回（リトライなし）。`notify_error` が1回呼ばれ、キーワード引数 `error_code` が `"GOLD_ERR_001"` である。`aggregate_sensor_data` は呼ばれない。`merge_to_gold` は呼ばれない。     |
| 10  | 1.3.1 例外伝播 / 2.3.2 例外発生時       | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が毎回 `RuntimeError("Network error")` を送出するように設定する。`pytest.raises(RuntimeError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                                                                               | `RuntimeError` が上位に伝播する。`filter` の呼び出し回数が3回（MAX_READ_RETRY 上限）。`notify_error` が1回呼ばれ、キーワード引数 `error_code` が `"GOLD_ERR_001"` である。`aggregate_sensor_data` は呼ばれない。`merge_to_gold` は呼ばれない。 |
| 11  | 1.3.1 例外伝播 / 2.3.2 例外発生時       | `merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` が `RuntimeError("Aggregation failed")` を送出するように設定する。`pytest.raises(RuntimeError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                               | `RuntimeError` が上位に伝播する。`notify_error` が1回呼ばれ、キーワード引数 `error_code` が `"GOLD_ERR_002"` である。`merge_to_gold` は呼ばれない。                                                                                            |
| 12  | 1.3.1 例外伝播 / 2.3.2 例外発生時       | `notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` が MagicMock を返すように設定する。`merge_to_gold` が `RuntimeError("Merge failed")` を送出するように設定する。`pytest.raises(RuntimeError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                   | `RuntimeError` が上位に伝播する。`notify_error` が1回呼ばれ、キーワード引数 `error_code` が `"GOLD_ERR_003"` である。                                                                                                                          |
| 13  | 1.4.1.1 ERRORレベル出力                 | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `AnalysisException("Table not found")` を送出するように設定する。`caplog.at_level(logging.ERROR)` コンテキスト内で `pytest.raises(AnalysisException)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                     | caplog のレコードの中に `"GOLD_ERR_001"` を含む ERROR レベルのメッセージが存在する。                                                                                                                                                           |
| 14  | 1.4.1.1 ERRORレベル出力                 | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `PermissionError("Access denied")` を送出するように設定する。`caplog.at_level(logging.ERROR)` コンテキスト内で `pytest.raises(PermissionError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                           | caplog のレコードの中に `"GOLD_ERR_001"` を含む ERROR レベルのメッセージが存在する。                                                                                                                                                           |
| 15  | 1.4.1.1 ERRORレベル出力                 | `aggregate_sensor_data`・`merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が毎回 `RuntimeError("Network error")` を送出するように設定する。`caplog.at_level(logging.ERROR)` コンテキスト内で `pytest.raises(RuntimeError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                                                                             | caplog のレコードの中に `"GOLD_ERR_001"` を含む ERROR レベルのメッセージが存在する。                                                                                                                                                           |
| 16  | 1.4.1.1 ERRORレベル出力                 | `merge_to_gold`・`notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` が `RuntimeError("Aggregation failed")` を送出するように設定する。`caplog.at_level(logging.ERROR)` コンテキスト内で `pytest.raises(RuntimeError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。                             | caplog のレコードの中に `"GOLD_ERR_002"` を含む ERROR レベルのメッセージが存在する。                                                                                                                                                           |
| 17  | 1.4.1.1 ERRORレベル出力                 | `notify_error` をモック化する。`spark.table().filter()` が `isEmpty=False` を返す MagicMock を設定する。`aggregate_sensor_data` が MagicMock を返すように設定する。`merge_to_gold` が `RuntimeError("Merge failed")` を送出するように設定する。`caplog.at_level(logging.ERROR)` コンテキスト内で `pytest.raises(RuntimeError)` を用いて `run_aggregation(AggregationPeriod.DAILY, "event_date = '2026-01-28'")` を呼び出す。 | caplog のレコードの中に `"GOLD_ERR_003"` を含む ERROR レベルのメッセージが存在する。                                                                                                                                                           |

---

### TestTeamsNotifier

| No  | テスト観点                                 | 操作手順                                                                                                                                                                                                                                                                                                                                    | 予想結果                                                                                                                                                                                                                                     |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 18  | 2.1.1 正常処理                             | `requests.post` が `status_code=200` を返す MagicMock を設定する。`TeamsNotifier(webhook_url, timeout=30, retry_count=3)` を生成する。`send_error_notification(error_code="GOLD_ERR_001", error_message="シルバー層からのデータ読込に失敗しました", pipeline_name="gold_sensor_data_daily_summary", target_date="2026-01-28")` を呼び出す。 | `True` を返す。`requests.post` が1回呼ばれる。`requests.post` の第1引数（位置引数）が `webhook_url` である。                                                                                                                                 |
| 19  | 2.1.1 正常処理 / 1.4.1.3 INFOレベル出力    | `requests.post` が `status_code=200` を返す MagicMock を設定する。`TeamsNotifier(webhook_url, timeout=30, retry_count=3)` を生成する。`caplog.at_level(logging.INFO)` コンテキストで `send_error_notification(...)` を呼び出す。                                                                                                            | caplog のレコードの中に「Teams通知送信成功」を含む INFO レベルのメッセージが存在する。                                                                                                                                                       |
| 20  | 2.1.1 正常処理                             | `requests.post` が `status_code=500` を返す MagicMock を設定する。`time.sleep` をモック化する。`TeamsNotifier(webhook_url, timeout=30, retry_count=2)` を生成する。`send_error_notification(...)` を呼び出す。                                                                                                                              | `False` を返す。`requests.post` が2回呼ばれる（retry_count=2）。`time.sleep(1)` が1回呼ばれる（attempt=0 のバックオフ: 2^0=1秒、attempt=1 は最後のため sleep なし）。                                                                        |
| 21  | 2.1.1 正常処理 / 1.4.1.2 WARNレベル出力    | `requests.post` が `status_code=500` を返す MagicMock を設定する。`time.sleep` をモック化する。`TeamsNotifier(webhook_url, timeout=30, retry_count=2)` を生成する。`caplog.at_level(logging.WARN)` コンテキストで `send_error_notification(...)` を呼び出す。                                                                               | caplog のレコードの中に「Teams通知送信失敗」を含む WARN レベルのメッセージが存在する。                                                                                                                                                       |
| 22  | 1.3.1 例外伝播                             | `requests.post` が `requests.exceptions.RequestException("Connection error")` を送出するように設定する。`time.sleep` をモック化する。`TeamsNotifier(webhook_url, timeout=30, retry_count=2)` を生成する。`send_error_notification(...)` を呼び出す。                                                                                        | `False` を返す。`requests.post` が2回呼ばれる（retry_count=2）。`time.sleep(1)` が1回呼ばれる（attempt=0 のバックオフ: 2^0=1秒、attempt=1 は最後のため sleep なし）。                                                                        |
| 23  | 1.3.1 例外伝播 / 1.4.1.2 WARNレベル出力    | `requests.post` が `requests.exceptions.RequestException("Connection error")` を送出するように設定する。`time.sleep` をモック化する。`TeamsNotifier(webhook_url, timeout=30, retry_count=2)` を生成する。`caplog.at_level(logging.WARN)` コンテキストで `send_error_notification(...)` を呼び出す。                                         | caplog のレコードの中に「Teams通知送信エラー」を含む WARN レベルのメッセージが存在する。                                                                                                                                                     |
| 24  | 2.3.2 例外発生時 / 1.4.1.1 ERRORレベル出力 | `requests.post` が `requests.exceptions.RequestException("Connection error")` を送出するように設定する。`time.sleep` をモック化する。`TeamsNotifier(webhook_url, timeout=30, retry_count=3)` を生成する。`caplog.at_level(logging.ERROR)` コンテキストで `send_error_notification(...)` を呼び出す。                                        | `False` を返す。caplog のレコードの中に「Teams通知送信失敗（リトライ上限）」を含む ERROR レベルのメッセージが存在する。`time.sleep` が2回呼ばれる（retry_count=3: attempt0→sleep(1)、attempt1→sleep(2)、attempt2 は最後のため sleep なし）。 |

---

### TestBuildAdaptiveCard

| No  | テスト観点                                   | 操作手順                                                                                                                                                                                                                                                                                                                                                                                          | 予想結果                                                                                                                                                                                                                                                                                         |
| --- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 25  | 2.1.1 正常処理                               | `TeamsNotifier(webhook_url)` を生成する。`_build_adaptive_card(error_code="GOLD_ERR_001", error_message="テストエラー", pipeline_name="gold_sensor_data_daily_summary", target_date="2026-01-28")` を呼び出す。`card["attachments"][0]["content"]["body"]` の中から `type="TextBlock"` の先頭ブロックを取得する。                                                                                 | 先頭 TextBlock の `text` に「[高]」が含まれる。先頭 TextBlock の `color` が `"attention"` である。                                                                                                                                                                                               |
| 26  | 2.1.1 正常処理                               | `TeamsNotifier(webhook_url)` を生成する。`_build_adaptive_card(error_code="GOLD_WARN_002", error_message="スキップ警告", pipeline_name="gold_sensor_data_daily_summary", target_date="2026-01-28")` を呼び出す。body の中から `type="TextBlock"` の先頭ブロックを取得する。                                                                                                                       | 先頭 TextBlock の `text` に「[中]」が含まれる。先頭 TextBlock の `color` が `"warning"` である。                                                                                                                                                                                                 |
| 27  | 2.1.1 正常処理 / CL-1-1 全フィールドアサート | `TeamsNotifier(webhook_url)` を生成する。`_build_adaptive_card(error_code="GOLD_ERR_001", error_message="シルバー層からのデータ読込に失敗しました", pipeline_name="gold_sensor_data_daily_summary", target_date="2026-01-28")` を呼び出す。body の中から `type="FactSet"` ブロックを取得し、`facts` リストを `title: value` の辞書に変換する。                                                    | `facts["エラーコード"]` が `"GOLD_ERR_001"` である。`facts["エラー内容"]` が `"シルバー層からのデータ読込に失敗しました"` である。`facts["パイプライン"]` が `"gold_sensor_data_daily_summary"` である。`facts["処理対象日"]` が `"2026-01-28"` である。`"発生日時"` キーが facts 内に存在する。 |
| 28  | 2.1.1 正常処理                               | `TeamsNotifier(webhook_url)` を生成する。長さ 614 文字のスタックトレース（`"Traceback...\n" + "x" * 600`）を用意する。`_build_adaptive_card(error_code="GOLD_ERR_001", error_message="エラー", pipeline_name="gold_sensor_data_daily_summary", target_date="2026-01-28", stack_trace=long_trace)` を呼び出す。body から `type="TextBlock"` かつ `text` に `"詳細情報"` を含むブロックを取得する。 | 詳細情報ブロックが存在する。同ブロックの `text` にスタックトレース先頭500文字（`long_trace[:500]`）が含まれる。同ブロックの `text` にスタックトレース501文字目以降（`long_trace[500:]`）が含まれない。                                                                                           |

---

### TestNotifyError

| No  | テスト観点     | 操作手順                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 予想結果                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 29  | 2.1.1 正常処理 | `pipeline.gold.gold_ldp_pipeline.dbutils` と `pipeline.gold.gold_ldp_pipeline.TeamsNotifier` をモック化する。`mock_dbutils.secrets.get` の戻り値を `"https://teams.webhook.example.com/test"` に設定する。`mock_notifier_class` の戻り値（インスタンス）を `mock_notifier_instance` に設定する。`notify_error(error_code="GOLD_ERR_001", error_message="テストエラー", pipeline_name="gold_sensor_data_daily_summary", target_date="2026-01-28")` を呼び出す。 | `mock_dbutils.secrets.get` が `scope="iot-pipeline-secrets"`, `key="teams-webhook-url"` の引数で1回呼ばれる。`TeamsNotifier` が `"https://teams.webhook.example.com/test"` の引数で1回生成される。`mock_notifier_instance.send_error_notification` が `error_code="GOLD_ERR_001"`, `error_message="テストエラー"`, `pipeline_name="gold_sensor_data_daily_summary"`, `target_date="2026-01-28"` の引数で1回呼ばれる。 |

---

## 変更履歴

| 日付       | 版数 | 変更内容                                                                                                              | 担当者       |
| ---------- | ---- | --------------------------------------------------------------------------------------------------------------------- | ------------ |
| 2026-04-24 | 1.0  | 初版作成                                                                                                              | Kei Sugiyama |
| 2026-04-24 | 1.1  | TestRunAggregation（ERRORログ確認5件）・TestAggregateSensorData・TestMergeToGold・TestNotifyError を追加（23件→34件） | Kei Sugiyama |
| 2026-04-27 | 1.2  | 対応表・カバレッジ表の誤りを修正（Model層クラスの誤混入削除、TestTeamsNotifier〜TestNotifyError の項目No.を正しく修正）合計29件 | Kei Sugiyama |
