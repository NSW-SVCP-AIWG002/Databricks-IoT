# テスト項目書 デバイス管理

**対象テストファイル**: `tests/integration/devices/test_device_routes.py`
**参照観点表**: `docs/05-testing/integration-test/integration-test-perspectives.md`

---

## TestDeviceListDisplay

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 4.1.1 / 2.1.1 データ0件（一覧表示）/ 一覧初期表示 | セッションフィクスチャでデバイスデータが投入済みの状態で、GET /admin/devices にアクセスする | HTTPステータスコード 200 が返る |
| 2 | 4.1.3 データ複数件表示 | セッションフィクスチャで「結合テストデバイスA」が投入済みの状態で、GET /admin/devices にアクセスする | HTTPステータスコード 200 が返り、レスポンスボディに「結合テストデバイスA」が含まれる |
| 3 | 4.1.4 論理削除レコード非表示 | delete_flag=True の「結合テスト削除済みデバイス」が投入済みの状態で、GET /admin/devices にアクセスする | HTTPステータスコード 200 が返り、レスポンスボディに「結合テスト削除済みデバイス」が含まれない |
| 4 | 4.1.5 データスコープフィルタ適用 | device_master のみに新規デバイスを挿入し（v_device_master_by_user には挿入しない）スコープ外デバイスを作成した後、GET /admin/devices にアクセスする。テスト後は device_master から削除する | HTTPステータスコード 200 が返り、スコープ外デバイス名がレスポンスボディに含まれない |

---

## TestDeviceDetailDisplay

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 5 | 4.2.1 / 2.1.5 存在するレコード / 参照画面表示 | セッションフィクスチャの uuid_a を使用して、GET /admin/devices/{uuid_a} にアクセスする | HTTPステータスコード 200 が返る |
| 6 | 4.2.5 関連データ表示 | セッションフィクスチャの uuid_a を使用して、GET /admin/devices/{uuid_a} にアクセスする | HTTPステータスコード 200 が返り、レスポンスボディに「結合テストデバイスA」が含まれる |
| 7 | 4.2.2 / 2.2.4 存在しないレコード / 存在しないリソース | 存在しない device_uuid「NONEXISTENT-UUID-XXXXX」を使用して、GET /admin/devices/NONEXISTENT-UUID-XXXXX にアクセスする | HTTPステータスコード 404 が返る |
| 8 | 4.2.3 論理削除済みレコード | セッションフィクスチャの uuid_del（delete_flag=True）を使用して、GET /admin/devices/{uuid_del} にアクセスする | HTTPステータスコード 404 が返る |
| 9 | 4.2.4 データスコープ外レコード | device_master のみに新規デバイス（oos_uuid）を挿入し v_device_master_by_user には挿入しない（user_id=1 エントリなし）後、GET /admin/devices/{oos_uuid} にアクセスする。テスト後は device_master から削除する | HTTPステータスコード 404 が返る |

---

## TestDeviceCreateForm

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 10 | 2.1.3 登録画面表示 | GET /admin/devices/create にアクセスする | HTTPステータスコード 200 が返る |

---

## TestDeviceCreate

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 11 | 4.3.1 / 2.3.1 正常登録 / 登録成功後リダイレクト | UnityCatalogConnector をモック化する。device_uuid（ユニーク値）、device_name（「新規登録テストデバイス」）、device_type_id=1、device_model（「MODEL-NEW」）、organization_id=2 を含む有効なフォームデータで POST /admin/devices/register に送信する。テスト後は作成レコードを device_master から削除する | HTTPステータスコード 302 が返り、Location ヘッダーに /admin/devices が含まれる |
| 12 | 4.3.1 正常登録（DBレコード確認） | UnityCatalogConnector をモック化する。device_uuid（ユニーク値）、device_name（「DB確認テストデバイス」）、device_type_id=1、device_model、organization_id=2 を含むフォームデータで POST /admin/devices/register に送信する。テスト後は作成レコードを device_master から削除する | device_master テーブルに送信した device_name（「DB確認テストデバイス」）のレコードが追加されている |
| 13 | 4.3.7 作成者設定 | UnityCatalogConnector をモック化する。device_uuid（ユニーク値）、device_name（「作成者確認デバイス」）、device_type_id=1、device_model、organization_id=2 を含むフォームデータで POST /admin/devices/register に送信する。テスト後は作成レコードを device_master から削除する | device_master の creator カラムがログインユーザーの user_id（1）で設定される |
| 14 | 4.3.1 / 4.3.4 / 4.3.7 正常登録（全カラム保存確認） | UnityCatalogConnector をモック化する。device_uuid（ユニーク値）、device_name（「全カラム確認デバイス」）、device_type_id=1、device_model（「MODEL-ALLCOL」）、sim_id（「SIM12345」）、mac_address（空）、device_location（「テスト設置場所」）、organization_id=2、certificate_expiration_date（「2030-12-31」）を含むフォームデータで POST /admin/devices/register に送信する。テスト後は作成レコードを device_master から削除する | device_master に登録され、device_uuid・device_name・device_type_id=1・device_model・sim_id がフォーム入力値と一致し、mac_address が NULL、device_location・organization_id=2・certificate_expiration_date が「2030-12-31」と一致し、creator=1・modifier=1・delete_flag=False（または 0）である |
| 15 | 3.1.2 必須チェック（エラー）device_uuid 未入力 | device_uuid を空文字にして、device_name・device_type_id=1・device_model・organization_id=2 は有効な値で POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 16 | 3.1.2 必須チェック（エラー）device_name 未入力 | device_name を空文字にして、device_uuid（ユニーク値）・device_type_id=1・device_model・organization_id=2 は有効な値で POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 17 | 3.1.2 必須チェック（エラー）device_type_id 未選択 | device_type_id を空文字にして、device_uuid（ユニーク値）・device_name・device_model・organization_id=2 は有効な値で POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 18 | 3.1.2 必須チェック（エラー）organization_id 未選択 | organization_id を空文字にして、device_uuid（ユニーク値）・device_name・device_type_id=1・device_model は有効な値で POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 19 | 3.1.2 必須チェック（エラー）device_model 未入力 | device_model を空文字にして、device_uuid（ユニーク値）・device_name・device_type_id=1・organization_id=2 は有効な値で POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 20 | 3.2.2 文字列長チェック（エラー）device_uuid 128文字超 | device_uuid に「A」を 129 文字入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 21 | 3.2.2 文字列長チェック（エラー）device_name 100文字超 | device_name に「あ」を 101 文字入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 22 | 3.2.2 文字列長チェック（エラー）device_model 100文字超 | device_model に「M」を 101 文字入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 23 | 3.2.2 文字列長チェック（エラー）sim_id 20文字超 | sim_id に「1」を 21 文字入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 24 | 3.6.2 不整値チェック（エラー）device_uuid 使用不可文字 | device_uuid に「DEV_001@invalid#」（アンダースコア・@・# を含む、英数字・ハイフン以外の文字）を入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 25 | 3.7.2 重複チェック（エラー）device_uuid 重複 | セッションフィクスチャで既に登録済みの uuid_a を device_uuid として POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 26 | 3.6.2 不整値チェック（エラー）MACアドレス形式不正 | mac_address に「INVALID-MAC」（XX:XX:XX:XX:XX:XX 形式でない）を入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 27 | 3.4.2 日付形式チェック（エラー） | certificate_expiration_date に「not-a-date」（日付形式でない文字列）を入力して POST /admin/devices/register に送信する | HTTPステータスコード 400 が返る |
| 28 | 2.3.4 成功メッセージモーダル表示 | UnityCatalogConnector をモック化する。有効なフォームデータで POST /admin/devices/register に follow_redirects=True で送信する。テスト後は作成レコードを device_master から削除する | HTTPステータスコード 200 が返り、レスポンスボディに「デバイスを登録しました」が含まれる |
| 98 | 4.3.5 自動採番（主キー） | UnityCatalogConnector をモック化する。有効なフォームデータで POST /admin/devices/register に送信する。その後 device_master から device_id を取得する。テスト後は作成レコードを削除する | device_master の device_id が正の整数（> 0）として自動採番されている |
| 99 | 4.3.6 作成日時設定（create_date / update_date） | UnityCatalogConnector をモック化する。登録実行前の datetime.now() を記録後、有効なフォームデータで POST /admin/devices/register に送信する。その後 device_master の create_date・update_date を取得する。テスト後は作成レコードを削除する | device_master の create_date・update_date がいずれも登録実行前の日時以降に設定されている |

---

## TestDeviceUpdate

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 29 | 2.1.4 更新画面表示 | mutable_test_device フィクスチャで作成したデバイスの device_uuid で GET /admin/devices/{dev_uuid}/edit にアクセスする | HTTPステータスコード 200 が返る |
| 30 | 4.4.2 部分更新（現在値の初期表示） | mutable_test_device の device_uuid で GET /admin/devices/{dev_uuid}/edit にアクセスする | HTTPステータスコード 200 が返り、レスポンスボディに現在のデバイス名が含まれる |
| 31 | 4.4.6相当 存在しないレコード（編集フォーム） | 存在しない device_uuid「NONEXISTENT-EDIT-UUID」で GET /admin/devices/NONEXISTENT-EDIT-UUID/edit にアクセスする | HTTPステータスコード 404 が返る |
| 32 | 4.4.1 / 2.3.2 正常更新 / 更新成功後リダイレクト | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid に対して device_name（「更新後デバイス名」）、device_type_id=1、device_model（「MODEL-UPDATED」）、device_location（「更新後設置場所」）、organization_id=2 を含む有効なフォームデータで POST /admin/devices/{dev_uuid}/update に送信する | HTTPステータスコード 302 が返り、Location ヘッダーに /admin/devices が含まれる |
| 33 | 4.4.1 正常更新（DBレコード確認） | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid に対して device_name（「DB変更確認デバイス名」）等の有効なフォームデータで POST /admin/devices/{dev_uuid}/update に送信する | device_master の device_name が「DB変更確認デバイス名」に更新されている |
| 34 | 4.4.1 / 4.4.2 / 4.4.5 正常更新（全カラム確認・不変カラム確認） | UnityCatalogConnector をモック化する。更新前の device_uuid と creator を取得後、device_name（「全カラム更新確認デバイス」）、device_type_id=1、device_model（「MODEL-UPD-ALL」）、sim_id（「SIM-UPD-001」）、mac_address（空）、device_location（「更新後設置場所」）、organization_id=2、certificate_expiration_date（「2031-06-30」）で POST /admin/devices/{dev_uuid}/update に送信する | device_master の変更カラム（device_name・device_model・sim_id・device_location・organization_id=2・certificate_expiration_date=「2031-06-30」・modifier=1）が更新値と一致し、device_uuid と creator が更新前と同じ値のまま不変である |
| 35 | 4.4.6 / 2.2.4 存在しないレコード更新 | UnityCatalogConnector をモック化する。存在しない device_uuid「NONEXISTENT-UUID」に対して POST /admin/devices/NONEXISTENT-UUID/update に送信する | HTTPステータスコード 404 が返る |
| 36 | 3.1.2 必須チェック（エラー・更新）device_name 未入力 | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid に対して device_name を空文字にして POST /admin/devices/{dev_uuid}/update に送信する | HTTPステータスコード 400 が返る |
| 37 | 4.4.4 更新者設定 | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid に対して有効なフォームデータで POST /admin/devices/{dev_uuid}/update に送信する | device_master の modifier カラムがログインユーザーの user_id（1）で設定される |
| 38 | 4.4.7 論理削除済みレコード更新 | UnityCatalogConnector をモック化する。セッションフィクスチャの uuid_del（delete_flag=True）に対して POST /admin/devices/{uuid_del}/update に送信する | HTTPステータスコード 404 が返る |
| 39 | 3.7.2 重複チェック（エラー・更新）MACアドレス重複 | MACアドレス「AA:BB:CC:DD:EE:FF」が設定された別デバイスを device_master に挿入する。UnityCatalogConnector をモック化する。mutable_test_device の device_uuid に対して mac_address に「AA:BB:CC:DD:EE:FF」を指定して POST /admin/devices/{dev_uuid}/update に送信する。テスト後は挿入した別デバイスを削除する | HTTPステータスコード 400 が返る |
| 95 | 4.4.1 成功メッセージモーダル表示 | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid に対して有効なフォームデータで POST /admin/devices/{dev_uuid}/update に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、レスポンスボディに「デバイスを更新しました」が含まれる |
| 96 | 4.4.3 更新日時更新 | UnityCatalogConnector をモック化する。更新実行前の datetime.now() を記録後、mutable_test_device の device_uuid に対して有効なフォームデータで POST /admin/devices/{dev_uuid}/update に送信する。その後 device_master の update_date を取得する | device_master の update_date が更新実行前の日時以降に更新されている（onupdate による自動設定） |
| 97 | 4.4.5 作成日時不変（create_date） | UnityCatalogConnector をモック化する。更新前の create_date を device_master から取得後、mutable_test_device の device_uuid に対して有効なフォームデータで POST /admin/devices/{dev_uuid}/update に送信する。その後 device_master の create_date を再取得する | device_master の create_date が更新前と同一の値のまま不変である |

---

## TestDeviceDelete

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 40 | 4.5.1 / 2.3.3 正常削除（論理削除）/ 削除成功後リダイレクト | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid を device_uuids として POST /admin/devices/delete に送信する | HTTPステータスコード 302 が返り、Location ヘッダーに /admin/devices が含まれる |
| 41 | 4.5.1 正常削除（論理削除フラグ確認） | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid を device_uuids として POST /admin/devices/delete に送信する | v_device_master_by_user の該当レコードの delete_flag が True（または 1）になっている |
| 42 | 4.5.2 存在しないレコード削除（削除対象未選択） | フォームデータに device_uuids を含めず POST /admin/devices/delete に送信する | HTTPステータスコード 302 が返る |
| 43 | 4.5.5 更新者設定（削除） | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid を device_uuids として POST /admin/devices/delete に送信する | device_master から modifier カラムを取得できる（レコードが存在する）。※要確認：設計書に delete_device の modifier 更新対象テーブルの明記なし |
| 44 | 4.5.2 存在しないレコード削除 | UnityCatalogConnector をモック化する。存在しない device_uuid「NONEXISTENT-UUID-FOR-DELETE」を device_uuids として POST /admin/devices/delete に送信する | HTTPステータスコード 302 が返り、Location ヘッダーに /admin/devices が含まれる |
| 45 | 4.5.3 既に論理削除済みレコード | UnityCatalogConnector をモック化する。セッションフィクスチャの uuid_del（delete_flag=True）を device_uuids として POST /admin/devices/delete に送信する | HTTPステータスコード 302 が返り、Location ヘッダーに /admin/devices が含まれる |
| 91 | 4.5.1 成功メッセージモーダル表示 | UnityCatalogConnector をモック化する。mutable_test_device の device_uuid を device_uuids として POST /admin/devices/delete に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、レスポンスボディに「件のデバイスを削除しました」が含まれる |
| 92 | 4.5.2 エラーメッセージ（存在しない UUID） | 存在しない device_uuid「NONEXISTENT-UUID-FOR-MSG-CHECK」を device_uuids として POST /admin/devices/delete に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、レスポンスボディに「指定されたデバイスが見つかりません」が含まれる |
| 93 | 4.5.3 エラーメッセージ（論理削除済みレコード） | セッションフィクスチャの uuid_del（delete_flag=True）を device_uuids として POST /admin/devices/delete に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、レスポンスボディに「指定されたデバイスが見つかりません」が含まれる |
| 94 | 4.5.4 更新日時設定 | UnityCatalogConnector をモック化する。削除実行前の datetime.now() を記録後、mutable_test_device の device_uuid を device_uuids として POST /admin/devices/delete に送信する。その後 device_master の update_date を取得する | device_master の update_date が削除実行前の日時以降に更新されている（onupdate による自動設定） |

---

## TestDeviceTransaction

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 46 | 7.3.2 Unity Catalog 失敗時ロールバック | UnityCatalogConnector の execute_dml に例外（Exception: "UC connection failed"）を発生させるようモック化する。ユニークな device_uuid を含む有効なフォームデータで POST /admin/devices/register に送信する | HTTPステータスコード 500 が返る。かつ、device_master に送信した device_uuid のレコードが存在しない（ロールバック済み） |

---

## TestDeviceExport

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 47 | 4.6.1 HTTPレスポンスヘッダー（Content-Type） | POST /admin/devices/export にアクセスする | HTTPステータスコード 200 が返り、Content-Type ヘッダーに「text/csv」が含まれる |
| 48 | 4.6.2 ファイル名（Content-Disposition） | POST /admin/devices/export にアクセスする | HTTPステータスコード 200 が返り、Content-Disposition ヘッダーに「attachment」および「devices_」が含まれる |
| 49 | 4.6.1 CSVヘッダー行確認 | POST /admin/devices/export にアクセスし、BOM（UTF-8）を除去した上でレスポンスボディをデコードする | HTTPステータスコード 200 が返り、CSVの内容に「デバイスID」が含まれる |
| 50 | 4.6.3 検索条件適用 | POST /admin/devices に device_name=「結合テストデバイスA」で送信し検索条件クッキーを設定する。その後 POST /admin/devices/export にアクセスする | HTTPステータスコード 200 が返り、CSVに「結合テストデバイスA」が含まれ、「結合テストデバイスB」が含まれない |
| 51 | 4.6.4 ソート順適用 | POST /admin/devices に sort_by=device_name・order=asc で送信しクッキーを設定する。その後 POST /admin/devices/export にアクセスする | HTTPステータスコード 200 が返り、CSV内で「結合テストデバイスA」が「結合テストデバイスB」より前の位置に出力される |
| 52 | 4.6.5 データ0件 | POST /admin/devices に存在しない device_name（「存在しないデバイス名ZZZZZZZZ」）で送信しクッキーを設定する。その後 POST /admin/devices/export にアクセスする | HTTPステータスコード 200 が返り、CSVにヘッダー行（「デバイスID」）は含まれるがデータ行（「結合テストデバイス」等）は含まれない。空行を除いたCSV行数が 1 である |
| 53 | 4.6.6 データスコープフィルタ適用 | device_master のみにスコープ外デバイスを挿入し v_device_master_by_user には挿入しない。POST /admin/devices で検索条件なしのクッキーを設定後、POST /admin/devices/export にアクセスする。テスト後はデバイスを device_master から削除する | HTTPステータスコード 200 が返り、CSVにスコープ外デバイス名が含まれない |
| 54 | 4.6.7 関連データ取得 | POST /admin/devices で device_name=「結合テストデバイスA」のクッキーを設定する。その後 POST /admin/devices/export にアクセスし、BOMを除去してCSVをパースする | HTTPステータスコード 200 が返り、ヘッダー行＋データ行が存在し、ヘッダー行・データ行ともに列数が 7（デバイスID・デバイス名・デバイス種別・設置場所・所属組織・証明書期限・ステータス）である |
| 55 | 4.6.8 全件出力（ページング無視） | クライアントのクッキーに per_page=1 を含む search_conditions_devices を直接設定する。その後 POST /admin/devices/export にアクセスする | HTTPステータスコード 200 が返り、per_page=1 に関わらず「結合テストデバイスA」と「結合テストデバイスB」の両方がCSVに含まれる |

---

## TestDeviceSearch

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 56 | 5.1.1 検索条件なし（リダイレクト確認） | device_id・device_name・device_type_id・location・organization_id・certificate_expiration_date・status・sort_by・order をすべて空文字にして POST /admin/devices に送信する | HTTPステータスコード 302 が返る |
| 57 | 5.1.7 部分一致検索（前方一致 リダイレクト確認） | device_name に「結合テスト」（部分一致する文字列）を入力し、他の検索条件は空文字にして POST /admin/devices に送信する | HTTPステータスコード 302 が返る |
| 58 | 5.3.1 1ページ目表示 | GET /admin/devices?page=1 にアクセスする | HTTPステータスコード 200 が返る |
| 59 | 5.3.2 2ページ目以降表示 | GET /admin/devices?page=2 にアクセスする（データ件数が 1 ページ以下でも正常応答することを確認） | HTTPステータスコード 200 が返る |
| 60 | 5.2.2 昇順ソート | GET /admin/devices?page=1 にアクセスする | HTTPステータスコード 200 が返る |
| 61 | 5.1.1 検索条件なし（内容確認） | device_id・device_name・device_type_id・location・organization_id・certificate_expiration_date・status・sort_by・order をすべて空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」「結合テスト別組織デバイス」がすべてレスポンスボディに含まれる |
| 62 | 5.1.2 完全一致検索 organization_id ヒット | organization_id=2 を指定し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれ、「結合テスト別組織デバイス」（org_id=3）が含まれない |
| 63 | 5.1.2 完全一致検索 device_type_id ヒット | device_type_id=1 を指定し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれる（全セッションデバイスがヒット） |
| 64 | 5.1.3 / 5.1.4 / 5.1.5 完全一致検索 非存在値でヒットなし | organization_id=99999（存在しない値）を指定し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がいずれもレスポンスに含まれない |
| 65 | 5.1.6 部分一致検索（device_name 完全一致文字列） | device_name に「結合テストデバイスA」（完全一致する文字列）を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」がレスポンスに含まれ、「結合テストデバイスB」が含まれない |
| 66 | 5.1.7 部分一致検索（device_name 前方一致 内容確認） | device_name に「結合テスト」（前方一致する文字列）を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれる |
| 67 | 5.1.8 部分一致検索（device_name 後方一致） | device_name に「デバイスA」（後方一致する文字列）を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」がレスポンスに含まれ、「結合テストデバイスB」が含まれない |
| 68 | 5.1.9 部分一致検索（device_name 中間一致） | device_name に「テストデバイス」（中間一致する文字列）を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれる |
| 69 | 5.1.7相当 部分一致検索（device_id 前方一致） | device_id に uuid_a の先頭 10 文字（前方一致する部分文字列）を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」がレスポンスに含まれる |
| 70 | 5.1.7相当 部分一致検索（location 中間一致） | device_location が「東京都テスト設置場所_{uid}」のテンポラリデバイスを device_master・v_device_master_by_user の両テーブルに挿入する。location に「テスト設置場所」（中間一致する文字列）を指定して POST /admin/devices に follow_redirects=True で送信する。テスト後はテンポラリデバイスを両テーブルから削除する | HTTPステータスコード 200 が返り、テンポラリデバイス名がレスポンスに含まれ、device_location=NULL の「結合テストデバイスA」が含まれない |
| 71 | 5.1.2相当 証明書期限検索（<=フィルタ ヒット・非ヒット） | certificate_expiration_date=「2028-06-30」のテンポラリデバイスを device_master・v_device_master_by_user の両テーブルに挿入する。①certificate_expiration_date=「2029-12-31」で POST /admin/devices に follow_redirects=True で送信（ヒット想定）、②「2027-01-01」で同様に送信（非ヒット想定）する。テスト後はテンポラリデバイスを両テーブルから削除する | ①：HTTPステータスコード 200 が返りテンポラリデバイス名がレスポンスに含まれる。②：HTTPステータスコード 200 が返りテンポラリデバイス名がレスポンスに含まれない |
| 72 | 5.1.2相当 ステータス検索（disconnected） | status=「disconnected」を指定し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、device_status_data に登録がない（last_received_time=NULL）「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれる |
| 73 | 5.1.10 AND検索（複数条件すべて満たすケース） | device_name に「結合テスト」、organization_id に「2」を指定して POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれ、「結合テスト別組織デバイス」（org_id=3）が含まれない |
| 74 | 5.1.11 AND検索（片方の条件しか満たさないケース） | device_name に「結合テストデバイスA」、organization_id に「3」を指定して POST /admin/devices に follow_redirects=True で送信する（両条件を同時に満たすデバイスは存在しない） | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テスト別組織デバイス」がいずれもレスポンスに含まれない |
| 75 | 5.3.3 最終ページ表示 | セッションフィクスチャの有効デバイスは 3 件（< 25 件）のため page=1 が最終ページとなる。GET /admin/devices?page=1 にアクセスする | HTTPステータスコード 200 が返り、「結合テストデバイスA」「結合テストデバイスB」がレスポンスに含まれる（最終ページに残りの件数が表示される） |
| 76 | 5.3.4 ページ範囲外（page=0） | GET /admin/devices?page=0 にアクセスする | HTTPステータスコード 500 以外が返る（サーバーエラーにならない）。設計期待値は 1 ページ目表示 |
| 77 | 5.3.5 ページサイズ境界値（25件） | ユニーク prefix で一時デバイス 22 件を device_master・v_device_master_by_user に挿入しセッション有効 3 件と合わせて計 25 件にする。GET /admin/devices?page=1 と page=2 にアクセスする。テスト後は一時デバイスを両テーブルから削除する | page=1：HTTPステータスコード 200 が返り一時デバイスの prefix がレスポンスに含まれる。page=2：HTTPステータスコード 200 が返り一時デバイスの prefix がレスポンスに含まれない（2 ページ目なし） |
| 78 | 5.3.6 ページサイズ境界値+1（26件） | ユニーク prefix で一時デバイス 23 件を device_master・v_device_master_by_user に挿入しセッション有効 3 件と合わせて計 26 件にする。GET /admin/devices?page=1 と page=2 にアクセスする。テスト後は一時デバイスを両テーブルから削除する | page=1・page=2 ともに HTTPステータスコード 200 が返る。page=1 と page=2 を合算したレスポンスに一時デバイスの prefix が含まれる（2 ページに分割される） |
| 79 | 5.3.7 総件数表示 | GET /admin/devices にアクセスする | HTTPステータスコード 200 が返り、レスポンスボディに `dm-result-count__value` クラス（総件数表示要素）が含まれる |

---

## TestDeviceAuth

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 80 | 1.2.1 システム保守者操作 | inject_test_user のデフォルト設定（user_type_id=1: system_admin）のまま GET /admin/devices にアクセスする | HTTPステータスコード 200 が返る |
| 81 | 1.2.7 サービス利用者操作（権限なし）登録 | before_request フックで g.current_user.user_type_id を 4（service_company_user）に設定する。device_uuid・device_name 等の有効なフォームデータで POST /admin/devices/register に送信する | HTTPステータスコード 403 が返る |
| 82 | 1.2.7 サービス利用者操作（権限なし）削除 | before_request フックで g.current_user.user_type_id を 4（service_company_user）に設定する。device_uuids を指定して POST /admin/devices/delete に送信する | HTTPステータスコード 403 が返る |
| 83 | 1.2.6 サービス利用者操作（権限あり）一覧参照 | before_request フックで g.current_user.user_type_id を 4（service_company_user）に設定する。GET /admin/devices にアクセスする | HTTPステータスコード 200 が返る |
| 84 | 1.2.4 販社ユーザー操作（権限あり）登録フォーム | before_request フックで g.current_user.user_type_id を 3（sales_company_user）に設定する。GET /admin/devices/create にアクセスする | HTTPステータスコード 200 が返る |

---

## TestDeviceSecurity

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 85 | 9.1.1 基本的なSQLインジェクション | device_name に「' OR '1'='1」を入力し、他の検索条件は空文字にして POST /admin/devices に送信する | HTTPステータスコード 200 または 302 が返る（500 エラーが発生しない） |
| 86 | 9.1.2 コメントを使ったSQLインジェクション | device_name に「'; DROP TABLE device_master--」を入力し、他の検索条件は空文字にして POST /admin/devices に送信する | HTTPステータスコード 500 以外が返る（エラーにならず正常処理される） |
| 87 | 9.2.1 基本的なXSS | device_name に「&lt;script&gt;alert('XSS')&lt;/script&gt;」を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、レスポンスボディに `<script>alert('XSS')</script>` がそのまま出力されない（Jinja2 自動エスケープにより `&lt;script&gt;` 等に変換される） |
| 88 | 9.1.3 UNIONを使ったSQLインジェクション | device_name に「' UNION SELECT 1,2,3--」を入力し、他の検索条件は空文字にして POST /admin/devices に送信する | HTTPステータスコード 500 以外が返る（エラーにならずリダイレクトされる） |
| 89 | 9.2.2 imgタグXSS | device_name に「&lt;img src=x onerror=alert('XSS')&gt;」を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返り、レスポンスボディに `<img src=x onerror=alert('XSS')>` がそのまま出力されない |
| 90 | 9.2.3 JavaScriptプロトコル | device_name に「javascript:alert('XSS')」を入力し、他の検索条件は空文字にして POST /admin/devices に follow_redirects=True で送信する | HTTPステータスコード 200 が返る（500 エラーが発生しない） |
