# テスト項目書 - アラート履歴

対象テストファイル: `tests/integration/alert-history/test_alert_history_routes.py`

対象エンドポイント:
- `GET  /alert/alert-history` 初期表示・ページング
- `POST /alert/alert-history` 検索実行
- `GET  /alert/alert-history/<alert_history_uuid>` 詳細表示（参照モーダル）

---

## TestAlertHistoryAuth

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 1 | 1.1.1 認証済みユーザー | inject_test_user autouse fixture により `g.current_user` が設定済みの状態で `GET /alert/alert-history` にアクセスする | ステータスコード 200 が返る |
| 2 | 1.1.2 未認証ユーザー | `auth_provider.get_user_info` が `UnauthorizedError("no auth header")` を送出するようモックし、`authenticate_request` を `before_request` に挿入した状態で `GET /alert/alert-history` にアクセスする | ステータスコード 401 が返る |
| 3 | 1.1.3 無効なトークン | `auth_provider.get_user_info` が `UnauthorizedError("invalid token")` を送出するようモックし、`X-MS-TOKEN-AAD-ACCESS-TOKEN: invalid.token.value` ヘッダーを付与して `GET /alert/alert-history` にアクセスする | ステータスコード 401 が返る |
| 4 | 1.1.4 セッション期限切れ | `auth_provider.get_user_info` が `UnauthorizedError("token expired")` を送出するようモックした状態で `GET /alert/alert-history` にアクセスする | ステータスコード 401 が返る |
| 5 | 1.1.1 認証済みユーザー | 認証済み状態で全条件空の検索データを `POST /alert/alert-history` に送信する | ステータスコード 200 が返る |
| 6 | 1.1.1 認証済みユーザー | `alert_history_test_data` の `history_uuid`（スコープ内・有効）を使用し、`GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 200 が返る |

---

## TestAlertHistoryListDisplay

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 7 | 2.1.1 一覧初期表示 | `page` パラメータなしで `GET /alert/alert-history` にアクセスする | ステータスコード 200 が返り、レスポンスに `html` 文字列が含まれる |
| 8 | 4.1.3 複数件 | `alert_history_test_data` を登録し（`history_acc`=1日前・`history_sub`=2日前）、`GET /alert/alert-history` にアクセスする | ステータスコード 200。レスポンス HTML に `alert_name_acc`（"テストアラートAH_アクセス可"）と `alert_name_sub`（"テストアラートAH_サブ"）が含まれる |
| 9 | 4.1.2 データ1件 | `device_name=device_name_acc` を条件に `POST /alert/alert-history` に検索リクエストを送信する（`history_acc` の1件のみヒット） | ステータスコード 200。`alert_name_acc` がレスポンスに含まれ、`1件` が表示される |
| 10 | 4.1.4 論理削除レコード非表示 | `delete_flag=True` の `history_deleted` を含むテストデータを登録し、`GET /alert/alert-history` にアクセスする | ステータスコード 200。レスポンス HTML に `history_deleted.alert_history_uuid` が含まれない |
| 11 | 4.1.1 データ0件 | `start_datetime='2099/01/01 00:00'`, `end_datetime='2099/01/02 00:00'`（遠い未来の期間）を条件に `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200。レスポンス HTML に `0件` が含まれる |
| 12 | 初期表示デフォルト日時フィルタ | 8日前の `AlertHistory`（`old_uuid`）を追加登録し、`GET /alert/alert-history`（`page` パラメータなし）にアクセスする | ステータスコード 200。`old_uuid` がレスポンスに含まれず、`alert_name_acc`（1日前）は含まれる |
| 13 | ClearCookie（初期表示時のCookieクリア） | 存在しないデバイス名（`'XXXXXXXXXX存在しないデバイス'`）で `POST` 検索してCookieを設定後、`GET /alert/alert-history`（`page` なし）でアクセスする | ステータスコード 200。Cookieがクリアされデフォルト検索条件が適用され、`alert_name_acc` がレスポンスに含まれる |

---

## TestAlertHistoryErrorTransition

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 14 | 2.2.4 存在しないリソース | DB に存在しない新規 UUID で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 404 が返る |
| 15 | 2.2.4 存在しないリソース | `'invalid-uuid-format'` という不正な UUID 形式で `GET /alert/alert-history/invalid-uuid-format` にアクセスする | ステータスコード 404 が返る |
| 16 | 2.2.5 サーバーエラー | `iot_app.views.alert.alert_history.search_alert_histories` を `RuntimeError("DB接続エラー（テスト）")` で patch し、`GET /alert/alert-history` にアクセスする | ステータスコード 500 が返る |
| 17 | 2.2.5 サーバーエラー | `iot_app.views.alert.alert_history.search_alert_histories` を `RuntimeError("DB接続エラー（テスト）")` で patch し、全条件空で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 500 が返る |

---

## TestAlertHistoryDetail

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 18 | 4.2.1 存在するレコード | `alert_history_test_data` の `history_uuid`（スコープ内・`delete_flag=False`）で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 200 が返る |
| 19 | 4.2.5 関連データ表示 | `history_uuid` で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 200。レスポンス HTML に `alert_name_acc` と `device_name_acc` が含まれる |
| 20 | 4.2.3 論理削除済みレコード | `history_deleted`（`delete_flag=True`）の UUID で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 404 が返る |
| 21 | 4.2.4 データスコープ外レコード | `history_inacc`（`org_inaccessible` 配下・スコープ外）の UUID で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 404 が返る |
| 22 | 4.2.2 存在しないレコード | DB に存在しない新規 UUID で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 404 が返る |

---

## TestAlertHistorySearch

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 23 | 5.1.1 / 2.1.2 検索条件なし・検索後表示 | 全条件空で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200 が返る |
| 24 | 5.1.1 検索条件なし（全データ表示） | `alert_history_test_data` を登録し、全条件空で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200。`alert_name_acc` と `alert_name_sub` がレスポンスに含まれる |
| 25 | 5.1.7 部分一致検索（前方一致）- デバイス名 | `device_name_acc`（"テストデバイスAH_アクセス可"）の先頭5文字を `device_name` に指定して `POST` 検索する | ステータスコード 200。`device_name_acc` がレスポンスに含まれる |
| 26 | 5.1.9 部分一致検索（中間一致）- デバイス名 | `'デバイスAH'`（`device_name_acc` の中間部分）を `device_name` に指定して `POST` 検索する | ステータスコード 200。`device_name_acc` がレスポンスに含まれる |
| 27 | 5.1.6〜5.1.9 部分一致検索（不一致）- デバイス名 | `'XXXXXXXXXX存在しないデバイス'` を `device_name` に指定して `POST` 検索する | ステータスコード 200。`device_name_acc` と `device_name_sub` がレスポンスに含まれない |
| 28 | 5.1.9 部分一致検索（中間一致）- アラート名 | `'テストアラートAH'`（`alert_name_acc` の部分文字列）を `alert_name` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 29 | 5.1.6 部分一致検索（完全一致）- アラート名 | `alert_name_acc` の完全名称を `alert_name` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる（LIKE '%完全一致%' でもヒット） |
| 30 | 5.1.7 部分一致検索（前方一致）- アラート名 | `alert_name_acc` の先頭6文字（"テストアラート"）を `alert_name` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 31 | 5.1.8 部分一致検索（後方一致）- アラート名 | `'AH_アクセス可'`（`alert_name_acc` の末尾部分）を `alert_name` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 32 | 5.1.2 完全一致検索（ヒット）- アラートレベルID | `alert_history_test_data` の `alert_level_id` を `alert_level_id` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 33 | 5.1.2 完全一致検索（ヒット）- アラートステータスID | `alert_history_test_data` の `alert_status_id` を `alert_status_id` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 34 | 5.1.10 複数条件AND検索（両方一致） | `device_name=device_name_acc`, `alert_name=alert_name_acc` を両方指定して `POST` 検索する | ステータスコード 200。`device_name_acc` がレスポンスに含まれる |
| 35 | 5.1.11 複数条件AND検索（片方不一致） | `device_name=device_name_acc`, `alert_name='XXXXXXXXXX存在しないアラート'` を指定して `POST` 検索する | ステータスコード 200。`device_name_acc` がレスポンスに含まれない（AND 条件により除外） |
| 36 | 5.1.1 日時範囲フィルタ（範囲内ヒット） | `start_datetime`=5日前・`end_datetime`=現在（`history_acc` の1日前を含む期間）を指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 37 | 5.1.1 日時範囲フィルタ（範囲外除外） | `start_datetime`=明日・`end_datetime`=明後日（テストデータより未来の期間）を指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` と `alert_name_sub` がレスポンスに含まれない |
| 38 | 5.1.7 部分一致検索（前方一致）- 設置場所 | `device_location_acc` の先頭7文字（"テスト設置場所"）を `device_location` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 39 | 5.1.9 部分一致検索（中間一致）- 設置場所 | `'設置場所AH'`（中間部分）を `device_location` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 40 | 5.1.6 部分一致検索（完全一致）- 設置場所 | `device_location_acc` の完全名称を `device_location` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる（LIKE '%完全一致%' でもヒット） |
| 41 | 5.1.8 部分一致検索（後方一致）- 設置場所 | `'場所AH'`（`device_location_acc` の末尾部分）を `device_location` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 42 | 5.1.6〜5.1.9 部分一致検索（不一致）- 設置場所 | `'XXXXXXXXXX存在しない設置場所'` を `device_location` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` と `alert_name_sub` がレスポンスに含まれない |
| 43 | 5.1.2 完全一致検索（不一致）- アラートステータスID | DB に存在しない `alert_status_id='99999'` を指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` と `alert_name_sub` がレスポンスに含まれない |
| 44 | 5.1.6 部分一致検索（完全一致）- デバイス名 | `device_name_acc` の完全名称を `device_name` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる（LIKE '%完全一致%' でもヒット） |
| 45 | 5.1.8 部分一致検索（後方一致）- デバイス名 | `'AH_アクセス可'`（`device_name_acc` の末尾部分）を `device_name` に指定して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 46 | 5.1.10 複数条件AND検索（全条件一致） | 期間・`device_name`・`device_location`・`alert_name`・`alert_level_id`・`alert_status_id` を `history_acc` の各属性値に合わせて全指定し `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれる |
| 47 | 5.1.11 複数条件AND検索（1件不一致） | 全条件を `history_acc` に合わせつつ `device_location` のみ `'XXXXXXXXXX存在しない設置場所'` に変更して `POST` 検索する | ステータスコード 200。`alert_name_acc` がレスポンスに含まれない（AND 条件） |

---

## TestAlertHistorySort

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 48 | 5.2.3 降順ソート | `sort_item_id=1`, `sort_order_id=2`（降順）で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200 が返る |
| 49 | 5.2.2 昇順ソート | `sort_item_id=1`, `sort_order_id=1`（昇順）で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200 が返る |
| 50 | 5.2.1 デフォルトソート | `sort_item_id=1`, `sort_order_id=-1`（指定なし）で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200 が返る |
| 51 | 5.2.3 降順ソート（順序確認）- 発生日時 | テストデータ登録（`history_acc`=1日前, `history_sub`=2日前）後、`sort_item_id=1`, `sort_order_id=2`（発生日時降順）で `POST` 検索する | ステータスコード 200。レスポンス HTML 内で `alert_name_acc`（1日前）の出現位置が `alert_name_sub`（2日前）より前（新しい順に並んでいる） |
| 52 | 5.2.2 昇順ソート（順序確認）- 発生日時 | テストデータ登録後、`sort_item_id=1`, `sort_order_id=1`（発生日時昇順）で `POST` 検索する | ステータスコード 200。レスポンス HTML 内で `alert_name_sub`（2日前）の出現位置が `alert_name_acc`（1日前）より前（古い順に並んでいる） |
| 53 | 5.2.3 降順ソート - sort_item_id=2（デバイス名） | `sort_item_id=2`, `sort_order_id=2`（デバイス名降順）で `POST` 検索する | ステータスコード 200 が返る |
| 54 | 5.2.3 降順ソート - sort_item_id=3（設置場所） | `sort_item_id=3`, `sort_order_id=2`（設置場所降順）で `POST` 検索する | ステータスコード 200 が返る |
| 55 | 5.2.3 降順ソート - sort_item_id=4（アラート名） | `sort_item_id=4`, `sort_order_id=2`（アラート名降順）で `POST` 検索する | ステータスコード 200 が返る |
| 56 | 5.2.3 降順ソート - sort_item_id=5（アラートレベル） | `sort_item_id=5`, `sort_order_id=2`（アラートレベル降順）で `POST` 検索する | ステータスコード 200 が返る |
| 57 | 5.2.3 降順ソート - sort_item_id=6（アラートステータス） | `sort_item_id=6`, `sort_order_id=2`（アラートステータス降順）で `POST` 検索する | ステータスコード 200 が返る |
| 58 | 5.2.2 昇順ソート - sort_item_id=2（デバイス名） | `sort_item_id=2`, `sort_order_id=1`（デバイス名昇順）で `POST` 検索する | ステータスコード 200 が返る |
| 59 | 5.2.2 昇順ソート - sort_item_id=3（設置場所） | `sort_item_id=3`, `sort_order_id=1`（設置場所昇順）で `POST` 検索する | ステータスコード 200 が返る |
| 60 | 5.2.2 昇順ソート - sort_item_id=4（アラート名） | `sort_item_id=4`, `sort_order_id=1`（アラート名昇順）で `POST` 検索する | ステータスコード 200 が返る |
| 61 | 5.2.2 昇順ソート - sort_item_id=5（アラートレベル） | `sort_item_id=5`, `sort_order_id=1`（アラートレベル昇順）で `POST` 検索する | ステータスコード 200 が返る |
| 62 | 5.2.2 昇順ソート - sort_item_id=6（アラートステータス） | `sort_item_id=6`, `sort_order_id=1`（アラートステータス昇順）で `POST` 検索する | ステータスコード 200 が返る |
| 63 | 5.2.2 昇順ソート（順序確認）- デバイス名 | `sort_item_id=2`, `sort_order_id=1`（デバイス名昇順）で `POST` 検索する | ステータスコード 200。`alert_name_acc`（"アクセス可"・ア）の出現位置が `alert_name_sub`（"サブ"・サ）より前（ア < サ） |
| 64 | 5.2.3 降順ソート（順序確認）- デバイス名 | `sort_item_id=2`, `sort_order_id=2`（デバイス名降順）で `POST` 検索する | ステータスコード 200。`alert_name_sub`（"サブ"・サ）の出現位置が `alert_name_acc`（"アクセス可"・ア）より前（サ > ア） |
| 65 | 5.2.2 昇順ソート（順序確認）- 設置場所 | `sort_item_id=3`, `sort_order_id=1`（設置場所昇順）で `POST` 検索する | ステータスコード 200。`alert_name_acc`（"テスト設置場所AH"）の出現位置が `alert_name_sub`（"テスト設置場所AHサブ"）より前（前方一致・短い方が先） |
| 66 | 5.2.3 降順ソート（順序確認）- 設置場所 | `sort_item_id=3`, `sort_order_id=2`（設置場所降順）で `POST` 検索する | ステータスコード 200。`alert_name_sub`（"テスト設置場所AHサブ"）の出現位置が `alert_name_acc`（"テスト設置場所AH"）より前 |
| 67 | 5.2.2 昇順ソート（順序確認）- アラート名 | `sort_item_id=4`, `sort_order_id=1`（アラート名昇順）で `POST` 検索する | ステータスコード 200。`alert_name_acc`（"アクセス可"・ア）の出現位置が `alert_name_sub`（"サブ"・サ）より前（ア < さ） |
| 68 | 5.2.3 降順ソート（順序確認）- アラート名 | `sort_item_id=4`, `sort_order_id=2`（アラート名降順）で `POST` 検索する | ステータスコード 200。`alert_name_sub`（"サブ"・さ）の出現位置が `alert_name_acc`（"アクセス可"・ア）より前（サ > ア） |

---

## TestAlertHistoryPagination

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 69 | 5.3.1 1ページ目表示（初期表示） | `page` パラメータなしで `GET /alert/alert-history` にアクセスする | ステータスコード 200 が返る |
| 70 | 5.3.1 1ページ目表示（?page=1） | `GET /alert/alert-history`（初期表示）で Cookie を設定後、`GET /alert/alert-history?page=1` にアクセスする | ステータスコード 200 が返る |
| 71 | 5.3.2 2ページ目以降表示（?page=2） | `GET /alert/alert-history`（初期表示）で Cookie を設定後、`GET /alert/alert-history?page=2` にアクセスする | ステータスコード 200 が返る（データ不足でも正常） |
| 72 | 5.3.2 2ページ目以降表示（Cookie検索条件引き継ぎ） | `device_name=device_name_acc` で `POST` 検索して Cookie に条件を保存後、`GET /alert/alert-history?page=1` でページングする | ステータスコード 200。Cookie の検索条件が引き継がれ、`device_name_acc` がレスポンスに含まれる |
| 73 | 5.3.4 ページ範囲外（page=0） | `POST` 検索で Cookie を設定後、`GET /alert/alert-history?page=0` にアクセスする | ステータスコード 200 が返る |
| 74 | 5.3.7 総件数表示 | `alert_history_test_data`（スコープ内非削除：`history_acc` + `history_sub` = 2件）を登録し、全条件空で `POST` 検索する | ステータスコード 200。レスポンス HTML に `2件` が含まれる |
| 75 | 5.3.5 ページサイズ境界値（25件） | 既存2件 + 追加23件 = 合計25件にして、全条件空で `POST` 検索する | ステータスコード 200。`25件` が表示され、`page=2` リンクが含まれない（1ページに収まる） |
| 76 | 5.3.6 ページサイズ境界値+1（26件） | 既存2件 + 追加24件 = 合計26件にして、全条件空で `POST` 検索する | ステータスコード 200。`26件` が表示され、`page=2` リンクが含まれる（2ページに分割） |
| 77 | 5.3.3 最終ページ表示 | 26件（既存2件 + 追加24件）登録後、`POST` 検索（Cookie設定）→ `GET /alert/alert-history?page=2` にアクセスする | ステータスコード 200。最古レコード（`extras[-1].alert_history_uuid`）が page=2 のレスポンスに含まれる |
| 78 | 5.3.2 2ページ目以降表示（ページ境界確認） | 26件登録後、`POST` 検索 → `GET page=1` と `GET page=2` を順に取得する | `newest_uuid`（最新）が page=1 のみに含まれ、`oldest_uuid`（最古）が page=2 のみに含まれる |
| 79 | SaveCookie→GetCookie（GET初期表示後のページング） | 26件登録後、`GET /alert/alert-history`（初期表示・SaveCookie）→ `POST` を経由せず `GET /alert/alert-history?page=2` にアクセスする | ステータスコード 200。page=2 のレスポンスに `oldest_uuid`（最古レコード）が含まれる |
| 80 | ClearCookie（POST検索の Cookie 上書き） | 1回目 `POST`（`device_name=device_name_acc` で絞り込み）→ 2回目 `POST`（`device_name=device_name_sub` で絞り込み）→ `GET /alert/alert-history?page=1` にアクセスする | ステータスコード 200。`alert_name_sub` が含まれ、`alert_name_acc` が含まれない（2回目の Cookie に上書きされている） |
| 81 | page=1 リセット（POST検索後） | 26件登録後、`POST` 検索 → `GET page=2` → 再度 `POST` 検索する | ステータスコード 200。再 `POST` のレスポンスに `newest_uuid`（最新・page=1）が含まれ、`oldest_uuid`（page=2）が含まれない |

---

## TestAlertHistoryDataScope

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 82 | 1.3.1 / 4.1.5 自組織データ表示 | `alert_history_test_data` を登録し、`GET /alert/alert-history` にアクセスする | ステータスコード 200。`alert_name_acc`（自組織配下デバイスのアラート）がレスポンスに含まれる |
| 83 | 1.3.2 / 4.1.5 下位組織データ表示 | `alert_history_test_data` を登録し、`GET /alert/alert-history` にアクセスする | ステータスコード 200。`alert_name_sub`（下位組織配下デバイスのアラート）がレスポンスに含まれる |
| 84 | 1.3.4 無関係組織データ非表示 | `alert_history_test_data` を登録し、`GET /alert/alert-history` にアクセスする | ステータスコード 200。`alert_name_inacc`（`organization_closure` 未登録組織のアラート）がレスポンスに含まれない |
| 85 | 1.3.4 無関係組織データ非表示（詳細アクセス） | `history_inacc`（スコープ外組織配下）の UUID で `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 404 が返る |
| 86 | 1.3.4 無関係組織データ非表示（検索時） | `alert_history_test_data` を登録し、全条件空で `POST /alert/alert-history` に検索リクエストを送信する（全件取得を試みる） | ステータスコード 200。`alert_name_inacc` がレスポンスに含まれない |
| 87 | 1.3.3 上位組織データ非表示 | `org_inaccessible`（上位組織相当・`organization_closure` 未登録）のアラート履歴が存在する状態で、全条件空で `POST` 検索する | ステータスコード 200。`alert_name_inacc` がレスポンスに含まれない |

---

## TestAlertHistorySecurity

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 88 | 9.1.1 基本的なSQLインジェクション | `device_name` に `' OR '1'='1` を指定して `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200 が返る（アプリがクラッシュしない） |
| 89 | 9.1.2 コメントを使ったSQLインジェクション | `device_name` に `'; DROP TABLE alert_history--` を指定して `POST` 検索する | ステータスコード 200 が返る |
| 90 | 9.1.3 UNIONを使ったSQLインジェクション | `alert_name` に `' UNION SELECT 1,2,3--` を指定して `POST` 検索する | ステータスコード 200 が返る |
| 91 | 9.2.1 基本的なXSS | `device_name` に `<script>alert('XSS')</script>` を指定して `POST` 検索し、レスポンス HTML を確認する | ステータスコード 200。レスポンスにスクリプトタグが平文で含まれない（Jinja2 自動エスケープにより除去） |
| 92 | 9.2.2 imgタグXSS | `device_name` に `<img src=x onerror=alert('XSS')>` を指定して `POST` 検索し、レスポンス HTML を確認する | ステータスコード 200。img タグペイロードが平文でレスポンスに含まれない |
| 93 | 9.2.3 JavaScriptプロトコル | `device_name` に `javascript:alert('XSS')` を指定して `POST` 検索し、レスポンス HTML を確認する | ステータスコード 200。JavaScriptプロトコルペイロードが平文でレスポンスに含まれない |
| 94 | 9.3.1 CSRFトークンなし | `app.config['WTF_CSRF_ENABLED'] = True` に設定し、CSRF トークンなしで `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 400 または 403 が返る |
| 95 | 9.3.2 無効なCSRFトークン | `WTF_CSRF_ENABLED=True` に設定し、`csrf_token='invalid-csrf-token-string'` を含めて `POST /alert/alert-history` にリクエストを送信する | ステータスコード 400 または 403 が返る |
| 96 | 9.3.3 有効なCSRFトークン（CSRF無効確認） | `WTF_CSRF_ENABLED=False`（テスト環境デフォルト）のままで、CSRF トークンなしで `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200 が返る（テスト環境では CSRF 無効のため正常処理） |

---

## TestAlertHistoryLog

| No | テスト観点 | 操作手順 | 予想結果 |
|----|-----------|---------|---------|
| 97 | 8.2.1 エラーログ | `iot_app.views.alert.alert_history.get_alert_history_detail` を `RuntimeError("DB接続エラー（テスト）")` で patch し、`logging.ERROR` レベルでキャプチャしながら `GET /alert/alert-history/{uuid}` にアクセスする | ステータスコード 404。ERROR レベルのログが1件以上出力され、メッセージに `アラート履歴` が含まれる |
| 98 | 8.1.5 ユーザー操作ログ | `logging.INFO` レベルでキャプチャしながら全条件空で `POST /alert/alert-history` に検索リクエストを送信する | ステータスコード 200。INFO ログが1件以上出力され、メッセージに `user_id` が含まれる |
| 99 | 8.1.1 処理開始ログ | `logging.INFO` レベルでキャプチャしながら `GET /alert/alert-history` にアクセスする | ステータスコード 200。INFO ログが1件以上出力される |
| 100 | 8.1.2 処理終了ログ | `logging.INFO` レベルでキャプチャしながら `GET /alert/alert-history` にアクセスする | ステータスコード 200。INFO ログが1件以上出力され、メッセージに `完了` が含まれる |
| 101 | 8.1.3 DBクエリログ | `logging.INFO` レベルでキャプチャしながら `GET /alert/alert-history` にアクセスする | ステータスコード 200。INFO ログのメッセージ全体に `取得開始` と `取得完了` が含まれる |
| 102 | 8.3.1 機密情報がマスキングされる | `logging.DEBUG` レベルでキャプチャしながら `GET /alert/alert-history` にアクセスする（`inject_test_user` の `email_address="test@example.com"`） | ステータスコード 200。全ログメッセージに `test@example.com` が平文で含まれない |
| 103 | 8.3.2 マスキング形式が正しい | `app.test_request_context()` 内で `get_logger('test.alert_history_masking')` を使い、`extra={'email': 'test@example.com'}` を指定して INFO ログを出力する | ログレコードの `email` フィールドが `te****@example.com` にマスキングされている |
